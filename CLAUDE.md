# Claude 项目入口

开始此项目的任何工作前，完整阅读根目录 `AGENTS.md` 与 `CODEX_CLAUDE_COLLABORATION.md`，遵守其中的安全边界、文件修改权和交接要求。

默认承担诊断与独立复核；只有用户明确分配实现任务后才修改约定范围内的文件。先检查当前代码与未提交差异，不假定 `cluade methods/` 中的旧建议仍未实现。

不得读取账户/资金、启用实盘、调用真实交易接口，或以安装同花顺数据 skill 为由扩大权限。不要覆盖 Codex 或用户的未提交工作。

本文件只是项目说明，不会自动启动 Claude 或外部协作。
