# M4-02B-RISK-EXIT-20260912

Owner: Claude in the existing ZK-trading / Fable 5.1 project advice (fork) session. Reviewer/integrator: Codex. The user authorized sequential small steps after independent acceptance, with the existing 15-minute patrol. Implement this one task and stop for review.

## Read first

Read AGENTS.md, CODEX_CLAUDE_COLLABORATION.md, THREE_YEAR_RESEARCH_EXECUTION_GOAL.md M4 acceptance, `_m4_20260912/PLAN.md`, current coordination state, M4_01_CODEX_ACCEPTANCE_20260912.md, M4_02A_CODEX_ACCEPTANCE_20260912.md, both execution_freeze.json and portfolio_freeze.json, and the final execution/ledger contracts, source and tests. Verify all freeze pins before writing. Historical manifests retain old coordination/PLAN hashes as historical reads; these Codex-owned mutable files naturally change when advancing tasks. Frozen source/evidence pins must still match.

Final kernel SHA: `83a28b543b9ecc5edf8080ea39fa388f8b2bb5a8724ef68b4b532041216678c7`.
Final ledger SHA: `2b3eec837e4c3603661371fb94e8d942f45c9bfadbf018fc5d6ebee6fadb5360`.
M4-02A passed 42 isolated tests and 10 independent Codex checks. This proves synthetic execution/accounting only. M3 remains 0/50 dual-positive, 32 disputed, strict_pit=false, training_eligible=false. M4_complete=false; no M5.

## Exclusive write scope

- NEW `backend/app/research/m4_risk.py`
- NEW `backend/tests/test_m4_risk.py`
- NEW `claude methods/_m4_20260912/claude_02b/` for isolated runner, synthetic scenarios, contracts and evidence.

Everything else is read-only: especially execution/ledger code and tests, both freezes, all M2/M3 files, original tracked modifications, production datasets/SQLite/knowledge, package initializers, APIs, legacy engine, settings and frontend. Codex owns coordination, task/acceptance files, baseline and automation. No SQLite connection, network/client/data access, account/fund/credential access, simulation screen clicks, service changes, other agents, Git staging/commit/push or training. Tests load modules by file path/dependency injection, with no app/pytest/conftest imports. If integration needs frozen changes, report a concrete limitation rather than changing frozen bytes.

## Deliverable: deterministic synthetic policy orchestration

Implement a small stdlib-only in-memory policy layer using the frozen kernel and ledger. Use explicitly injected observations, calendars, policy and hypothetical fee/lot/slippage assumptions. No optimization, ranking/model training, universe expansion or historical reads. Public output must keep review_only=true, live_trading_enabled=false, training_eligible=false and distinguish decisions, intents, attempts, fills and unresolved states.

1. **Causal valuation and sizing.** Every mark/signal/policy/evidence has symbol, observation and availability time, provenance and synthetic status. Both observation and availability must be no later than the decision time. Missing, stale under a declared maximum age, future or inconsistent marks produce an explicit refusal/incomplete valuation; do not silently drop held positions, use zero, cost basis, future close or latest-price fallback. Compute cash plus all held positions using eligible marks; declare gross exposure and per-symbol weights. Size buys deterministically to explicit cash, per-position and portfolio exposure limits, rounding to the injected lot policy and reserving hypothetical fees/slippage. An execution-time price gap must be rechecked against the limits before ledger application; no backdated resizing using a later close. Insufficient cash/room gives a documented zero-size decision, never a forced minimum lot. Multiple same-time signals need stable ordering and shared cash/exposure reservation or sequential recomputation; pending buy intents cannot each spend the same capacity. Clearly define how partial/unfilled/rejected/cancelled synthetic intents release or retain reservations. No arbitrary ledger delta bypass.

2. **Deterministic stop/exit/cooldown.** Choose one simple explicit fixed policy with parameters supplied by the caller (no tuning). Define entry-cost reference, stop threshold, optional profit target, maximum holding sessions, priority when exit reasons coincide, and session-count cooldown. Decisions at an observed close execute no earlier than the next legal point through the frozen execution contract. Do not fill at a threshold merely because a bar touches it; if within-bar ordering is unknown, reject the inference or use an explicitly conservative documented policy without optimistic fills. A gap uses the legally observed execution price and kernel limits. T+1, suspension, limits, expiry and liquidity still apply. Partial exits retain the correct residual position/pending state; cooldown starts only on the documented actual exit condition, never on an unfilled intent. Failed/duplicate attempts must not falsely close holdings or reset cooldown. Re-entry exactly at the declared cooldown boundary and after non-trading days must be deterministic.

3. **Benchmark and delisting boundaries.** Use an explicitly preselected injected benchmark series with the same decision/valuation horizon and evidence-time checks; never trade an index through the stock ledger. Distinguish missing benchmark observations from zero return and no-lookahead alignment from future interpolation. A fixed synthetic universe must retain a security that later suspends/delists. Delisting announcements become usable only when available; future terminal status cannot remove earlier trades or membership. Do not silently drop holdings or invent a final sale. Without a verified executable exit or settlement/corporate-action support in the frozen contracts, retain the unresolved position and report incomplete valuation/performance and the precise limitation. You may represent hypothetical terminal valuation scenarios separately, but never post unsupported liquidation cash or call them real execution. No survivorship-cleaned fixture selection.

4. **Meaningful controlled proof.** Provide one simple nonzero end-to-end fixture going through policy decision -> sizing -> kernel -> ledger -> exit/cooldown -> valuation/benchmark, plus manually calculated expected sizes, costs, cash, exposure and return values. Preserve explicit hypothetical labels. Include positive and adversarial tests for fee-aware rounding; exact exposure boundaries and gaps; simultaneous symbols/shared resources; missing/future held marks; next-session stop execution; gap/limit/suspension/partial exits; cooldown boundaries/duplicates; benchmark availability/missingness; future delisting announcement versus known delisting with an unresolved holding. Appending or altering information strictly after a decision must not change its prior outputs or audit hashes. Show that prior prefixes remain immutable. Numeric spellings and caller Decimal context must not affect results. Read views must be detached; rejected operations must be atomic.

Avoid broad infrastructure. The goal is a reviewable deterministic policy contract integrated with the existing pure modules, not a production service. Explicitly report any unsupported requirement; do not claim M4 is complete or fabricate historical nonzero trades.

## Evidence and stopping point

Before work record branch/HEAD, status and both freezes; preserve the original 316 tracked-file baseline, 42 immutable pins and 16 production file positions. Hash raw bytes only, no SQL. New writes stay in the exclusive scope. Guard test processes before loading subjects: deny SQLite, sockets, subprocesses and writes outside claude_02b, disable bytecode, self-check guards safely. Do not overwrite previous evidence.

Deliver CONTRACT.md (interfaces, units, causal boundaries, status/state transition rules, reservations, exit priority, benchmark/delist limitations), HAND_CALCULATED_BASELINE.md, actual isolated unittest stdout and JSON execution receipts, per-event scenario/reconciliation records, source/preservation verification and runner source. Run focused checks then the complete new suite once on final bytes; frozen suites may run read-only with output confined to claude_02b. Record actual commands, exit codes and counts; declarations are not execution evidence.

Write artifact_manifest.json LAST, pinning all written files and consumed sources including this task, both freezes, final source/test bytes and actual preservation receipts. Report `M4-02B-RISK-EXIT-20260912 ready_for_review` with source/test/manifest hashes, limitations and stop. No self-acceptance, further task dispatch, M4-03 historical access, M5, or automation changes.
