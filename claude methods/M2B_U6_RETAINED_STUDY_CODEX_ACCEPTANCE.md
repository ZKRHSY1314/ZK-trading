# U-6 retained study — bounded offline technical validation

2026-09-09. **Technically validated for the retained-JSON numerical-study scope at revision 2. US-R1, US-R2, US-R3 and US-R4 are closed for the hash below.** This does not certify a supplier algorithm, a vendor/reference price basis, M2 source capability, a three-year corpus or training readiness. No policy is adopted.

## Validated artifact and execution

`M2B_U6_RETAINED_STUDY.md`, SHA-256 **65fbe917be7816f725461ff3d6ed7f3f2f2bce2e3f27019ae8efabf2dde5cb41**. The completed Claude revision-2 handoff identifies this hash. The reviewed bytes, complete-program stdout and verification results are frozen under `_m2_codex_review/u6_study_review_20260909_r2/`.

Codex inspected the complete sentinel-delimited Python program before executing the extracted block in memory using the bundled Python with `-X utf8 -B`. Its only external reads are the two hash-pinned retained JSON files; it writes nothing. The program completed successfully and produced **14 interval rows and 7 conditional event comparisons**. Every interval count, reference-price range, difference spread, feasibility result and exact feasible bound matched the prior independent rational-arithmetic results. The updated exact-decimal and double-arithmetic residual columns reproduce. This is numerical analysis verification, not a production test-suite result.

All **91 protected file pins** and **46 G1 files** match. The three named scratch files exist at the reported paths and match the original reported hashes. Their creation in revision 1 departed from the single-file/in-memory assignment; revision 2 now records that fact accurately and the files remain untouched. Their preservation closes the reporting contradiction; this review does not retroactively authorize the earlier side effect or prove the absence of every other filesystem action. HEAD **73f266d**, staging empty. No network, SQLite, adapter replay, decoding, services or production mutation was used for this review.

## Corrected analysis and retained limits

- Complete executable code now replaces the missing fragments, asserts pins first, states the chosen representations, and reproduces the report from retained inputs.
- E-1's 0.28 observed step versus 0.27 disclosed amount remains an unresolved observation. Compatibility with the examined multiplicative mapping or a separately assumed latent additive step is not a causal explanation and does not establish either premise.
- Source comparisons remain descriptive. The study neither identifies which leg generated the pattern nor establishes that an internally uniform `qfq` label is reliable or unreliable.
- The half-cent nearest-rounding envelope, continuous feasible intervals, within-sample algebraic checks, and lack of an event-detection guarantee are stated explicitly. New representation-related residuals do not change any of the 14 interval outcomes.

Non-blocking wording: the isolated phrase “5-cent-looking bracket” beside `[86,88]` is confusing, but the same sentence and code correctly give continuous width 2 and three grid points; the C-step windows correctly have width 4. No new correction round for this wording. In the double-arithmetic residual column both operands are converted to binary floats; the exact-decimal column supplies the exact rational-versus-decimal comparison. Neither is an independent vendor observation.

Keep **P1 open, U-6 deferred, all eligibility false, source capability FAIL**, retained EV6 failures unchanged and both capture authorizations consumed. The conditional relation tests are complete for this assignment; do not keep fitting new relation families to the same evidence merely to prolong the task.

## Next authorized offline task

Proceed with a **reference-basis lineage audit**, using existing code and retained provenance as text/JSON only. This addresses the decisive remaining reference-basis question more directly than a further descriptive volume/amount study. Trace how each stock reference pipeline can produce its stored `qfq` label, and distinguish request arguments, response declarations, adapter transformation, cache fallback/defaults and historical migration. Current code behavior is not proof that it generated the retained rows; explicitly test what historical binding the retained evidence provides.

The next task's complete scope is `_m2_codex_review/reference_basis_lineage_dispatch_20260909.txt`. It may create only `M2B_REFERENCE_BASIS_LINEAGE_AUDIT.md`. It does not authorize database access, provider invocation, new data, code changes or a policy decision. Continue the 10-minute heartbeat under the user's existing continuous-offline authorization; no stop or stop-email is required while useful authorized work continues.

## English handoff

US-R1 through US-R4 are closed for the revision-2 study hash above. Preserve the study and all evidence. Execute REF-BASIS-LINEAGE-20260909 from the new dispatch file, using existing local source text and retained JSON only. Create only the specified independent audit document. Do not equate a request argument or migration-assigned label with independently verified supplier basis, and do not equate current code with a historically pinned producer. Hand off at proposed for review; Codex will independently review and continue authorized offline work.
