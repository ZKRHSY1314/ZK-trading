# M3 implementation and independent review

Status: technically_closed_evidence_target_not_met. M3_technical_complete=true; original research target_met=false; training_eligible=false. Historical plan follows. User instruction 2026-09-10: “开始M3吧，一样的流程”. Codex coordinates and independently verifies; Claude works only the current bounded handoff. Existing 15-minute heartbeat will be retargeted from completed M2 to M3 in this same Codex task.

M3 objective and acceptance remain THREE_YEAR_RESEARCH_EXECUTION_GOAL.md:187–208: causal label definitions and reviewed positive/negative/ambiguous/failed case library, target at least 50 independently reviewed positives with 3–5 matched controls each before a supervised result is more than exploratory. A passing module or one report is not M3 completion. No artificial balance, no hidden actor claims, no strict-PIT/training readiness inferred from M2.

Sequence:
1. Verify immutable M2 handoff and current dirty workspace; record a new baseline and inspect unique existing Claude session.
2. M3-01: Claude writes a new pure label policy/module and adversarial synthetic tests, without real-case selection. Codex independently reviews concrete semantics and cutoff/availability/split/matching choices.
3. After policy review, freeze policy and create an isolated, hash-bound reader/case run over the two M2 candidate stores read-only; original prices, knowledge and historical labels remain immutable. Write only new M3 outputs. Validate before/after pins and enumerate every exclusion.
4. Review each actual positive and matched-control case independently, preserve dissent and ambiguous/failed cases, audit dependency and effective sample counts, then address substantive findings in new versions. Evidence limitations are explicit and cannot be replaced by synthetic cases or duplicated positives.
5. Write final M3 acceptance only after its actual criteria are met or a genuine evidence limitation is established and honestly reported. Pause this M3 patrol only after verified completion or an unavoidable boundary, not after the first draft or a test count. Do not automatically start M4.

M2 handoff: M2_FINAL_ACCEPTANCE_20260910.md and _m2_codex_implementation_20260910/closure_review/completion.json. Old pre-review flags remain frozen. Technical closure is 52-security staging only; 14 warmup shortfalls, strict_pit=false, one qualified BJ total-scope interpretation and DOM-evidence limits remain material to downstream research.

Safety: review-only, simulation-only where applicable, live trading disabled. No production SQLite opens/writes, services, new capture, accounts/credentials/funds/orders, Git staging/commit/push/PR, existing labels/knowledge replacement or universe expansion. No existing service imports with constructors that initialize SQLite. New initial research labels use a separate namespace; changing previously adopted meanings or material research choices must be reviewed before dependent work.

Ownership: Claude owns only the files listed in M3_01_LABEL_SPEC_CLAUDE_TASK_20260910.md; Codex owns baseline/, codex/, PLAN.md, coordination_state.json and task/review documents. M2 coordination file and frozen artifacts are not repurposed; M3 uses its own state file and a retained M2-state snapshot.

## Final technical closure (2026-09-10)

M3-01, M3-02 and M3-03 completed actual independent technical review. The frozen inventory yielded32 matched episodes; each of two actual agent reviewers reviewed32 cases,127controls and8diagnostics. All32 case verdicts disagree; dual-positive count0/50. Original opinions and inputs are preserved. See ../M3_FINAL_ACCEPTANCE_20260910.md and codex/final_acceptance_01/completion.json. No active delegated task remains. The existing15-minute heartbeat may now be paused; no automatic M4, new capture, policy tuning or production mutation. This closes the bounded implementation and evidence-limitation branch, not the unmet supervised-research gate.
