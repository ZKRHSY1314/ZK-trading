# M2 最终技术验收 — 同花顺三年历史暂存语料

日期：2026-09-10。集成负责人：Codex。独立复核：Claude，既有 `ZK-trading / Fable 5.1 project advice (fork)` 会话。

**结论：M2 技术验收通过（`validated`），在用户已批准的 50 只股票与 2 个基准指数范围内完成三年历史暂存交付。** Claude 独立报告为 PASS，Codex 已核对其完整交付指纹，并在新目录重现其日期键、全量原始响应和复权对照结果。没有把本次 52 标的交付表述为全 A 股历史库完成，也没有把暂存数据完整性表述为 M3、训练、完整历史 PIT 或生产可用性。

用户最终 `accepted` 状态仍由用户确认；本文件记录技术验收结果，不代替用户确认。M2 技术工作已完成，后续自动化应暂停；实际协调和自动化状态以相邻收口回执为准。

## 1. 范围和授权依据

- 原目标 `THREE_YEAR_RESEARCH_EXECUTION_GOAL.md:173-185` 要求经授权建立可验证三年语料，先暂存、记录前后证据、拒绝错误日期与重复业务键、保持复权口径一致；生产迁移是另行授权的条件项。
- 冻结总体为 `pilot_symbols.csv` 的 50 只股票和 2 个指数，SHA-256 `97e251ae84fc927486127a96b4966ed8fc59fac0b744b7d6f186b2b1548886fe`。研究期为 2023-09-04 至 2026-09-04，共 728 个交易日。按上市日期确定逐标的有效区间。
- 用户回复“可以”采纳 `M2_ACCEPTANCE_REVISION_PROPOSAL.md`：真实价格与独立停牌登记互斥覆盖；为指定 3 只成熟股票补足 250 根真实暖启动观察；对有来源支持的大宗交易统计口径使用原 2% 界限；新文件、新运行、旧 FAIL 保留。对应 `acceptance_v2_authority.json` 为 Codex 对会话授权的记录，不是新授权来源。
- 用户随后明确要求“完成这一步以后写一个验收文档，接下来启动委派给Claude任务继续推进，维持15min巡检一次，直至完成M2”。已先完成实际暂存及 Codex 阶段文档，再向唯一既有 Claude 会话提交独立复核；没有重复派单或另建 Claude 会话。

## 2. 实际交付

运行号：`ths_v2_20260910_041710_97ef9c09`。

暂存根目录：`D:\codex-A股交易\claude methods\_m2_codex_implementation_20260910\staging_runs\ths_v2_20260910_041710_97ef9c09`。

候选子目录：`run_ths_v2_20260910_041710_97ef9c09`。发布的仅是该暂存根目录内的 `CURRENT.json`。

| 项目 | 每个库的实际结果 |
| --- | ---: |
| 标的 | 52 |
| 研究期真实价格 | 35,943 |
| 研究期全天停牌 | 250 |
| 暖启动真实价格 | 9,742 |
| 暖启动全天停牌 | 48 |
| 总价格行 | 45,685 |
| 独立停牌记录 | 298 |
| 完整覆盖清单 | 45,983 |
| 未知缺口、额外业务键、价格/停牌交集 | 均为 0 |
| 错误伪日期、重复业务键 | 均为 0 |

停牌不生成 OHLC、不前填价格、不以零冒充行情。两库保存同一批观察，不能相加成 91,370 根独立行情。

同花顺底层接口实际单个响应最多 978 根，52 个原始主响应中 49 个超过 500 根。底层长历史能力已由真实响应证明；日常适配器的 500 根限制不能充当底层限制。没有默认改走 Sina。

3 只补暖启动股票为 SZ002656、SH600110、SH600226，分别新增 39、8、1 根。补采范围 2022-05-01 至 2023-09-01，选择研究开始前最近 250 个符合合同的真实观察。原窗口内的全部旧价格保留；702 个重叠点的 4,212 次 OHLCVA 比较完全相同，186 个更早未选用点仍保留在原始响应中。

14 只上市年限不足股票的短缺保持原登记：BJ920001 为 167 根，BJ920006 为 75 根，其余 12 只为 0 根。本次只确认采集完整性，没有把这些股票声明为 250 根特征就绪。

## 3. M2 标准逐项验收

| 标准 | 证据与结论 |
| --- | --- |
| M1 复核后明确授权 | 原授权链、用户“可以”及后续继续完成/委派指令；总体未扩大，生产迁移未授权 |
| 先写暂存或副本 | 两个独立候选 SQLite，实际运行与发布回执存在；没有生产迁移 |
| 前后清单、数量、哈希、覆盖、拒收、来源 | 冻结交付清单 31 项、传递证据 479 项；完整日期库存、原始字节及逐行来源；补充 61 次历史请求的完整去向核对 |
| 无 ERROR 日期或重复业务键进入研究视图 | 两库全表检查均为 0；研究视图 35,943 行，均为不复权 `none` |
| 整段复权一致 | 固定原生请求协议与不复权存储；两个有现金除权事件的 0/1/2 模式对照各 978 日，独立现金前复权算式每标的 3,912 次 OHLC 比较无差异；全体股票逐日价格/量额一致性检查通过，含下节所限的一条市场口径解释 |
| 生产迁移原子/可恢复且独立复核 | 本次不适用：生产迁移未请求、未授权、未执行。暂存指针已独立核对 |

新研究门禁必需项 20 项 PASS。暖启动采集完整性必需项 24 项 PASS；V3b 按实际 14 只上市短缺继续为 FAIL，不冒称全部特征可用。原合同研究门禁 P4/M1/M3 FAIL 与暖启动旧 FAIL 的真实运行输出保留；新合同通过不改写旧结果。历史 Sina/AkShare EV6 FAIL、P1/U-6 状态及已耗尽的旧采集授权均保留。

## 4. 独立复核与最终重现

Claude 独立报告：`M2_TONGHUASUN_V2_CLAUDE_INDEPENDENT_REVIEW_20260910.md`，SHA-256 `63620379262de943448e96847922b2da1ac4ceed54d5abac22d723aaf78ad520`。独立交付目录为 `_m2_ths_v2_claude_review_20260910`；报告及 14 个输出文件、45 个输入文件均已重新核对哈希。

Claude 使用独立日期键推导程序，从交易日历、上市日期与已核读的停牌事实得到预期集合，未将候选价格反过来定义应有日期。其原始响应重放核对两库全部 45,685 行：每库 274,110 次 OHLCVA 比较，无数值差异、额外键或缺失键；逐行来源、请求/生产者/解析器/原始体哈希、观察时间和点下标一致。25 个停牌区间对应的 28 个证据文件完成哈希及边界复核。

Codex 在 `closure_review/reproduced` 新目录重新运行冻结的 R03、R05、R06 程序，三个程序均退出 0，完整 JSON 结果与 Claude 冻结输出相同。该操作是重现 Claude 方法，不声称是第三套独立实现。重现时仅允许精确指定的两个候选库以 `mode=ro` 打开，禁止网络与子进程，写入仅限新输出目录。全部源/候选/Claude 交付指纹在重现前后相同。

阶段实测保留：Codex 9 项新合同/边界测试通过；Claude 实际重跑其中 8 项并保留终端文本，未重跑启动子进程验证生产路径拒绝的第 9 项。实际隔离候选被故意改动 1 元成交额后，逐行核验明确拒绝且没有发布指针；该破坏性夹具不是交付候选。最终 `git diff --check` 退出 0，索引没有暂存文件，分支/HEAD 未变化。

本次实际重现命令（绝对路径，输出目录必须不存在）：

```powershell
& 'D:\codex-A股交易\backend\.venv\Scripts\python.exe' -B -X utf8 'D:\codex-A股交易\claude methods\_m2_codex_implementation_20260910\closure_review\verify_final.py'
```

Claude 报告第 2 节的相对命令示例省略了 `claude methods/`，且测试使用不同工作目录；不能将其路径文字直接作为仓库根目录下可复制的命令。本次绝对路径重现回执补足可执行路径证据。重现程序不会覆盖已经存在的输出目录。

## 5. 记录订正与证据强度

以下订正以本文件补充，原 Codex 报告、Claude 报告和资格包的冻结字节均不改写。

1. **名称乱码。** 所有价格行的 `row_evidence.source_name` 均为 NULL，异常诊断单独保存。异常名称原文在原始 `.bin` 响应中保留，不在数据库该名称列中保留。证券身份依靠代码及映射证据，不依赖乱码名称。两个基准的显示名称为空是非阻断展示问题。
2. **来源口径标志。** `scope_evidence_verified=True` 是构建器填写的资格字段，不是自动读取 PDF 内容得出的证明。保留 PDF 的第 12 页由 Codex 与 Claude 两个智能体分别核读；不能称为“两名人工审阅者”。代码校验文件哈希、证券/日期/大宗记录和算式，PDF 内容解释属于审阅判断。
3. **北交所仅一条限定解释。** BJ920006 / 2023-12-04 的日总量额原值未改写。北交所保留规则说明大宗量额计入日总量额，官方 DOM 观察为旧代码 837006 的 400,000 股、9.25 元成交；扣除推算的 3,700,000 元后，剩余均价 12.5353557045 元位于竞价高低价 12.15–12.75 内。结合原生总量/总额字段身份，这支持此业务键的审阅口径；它不是供应商对字段范围的直接说明，也不能仅靠平均价低于最低价就无条件“证明含大宗”。该推论依赖 OHLC、量额、单位和时间范围正确；数据错误也是逻辑上的可能解释。此次接受其为已批准暂存合同下、证据明确限定的来源解释，没有修改原始数值或放宽 2% 阈值。
4. **SSE 锚点范围。** 现有 SSE 单日单位锚点为竞价统计口径，与北交所日总量额规则属于不同市场/字段语境。Claude 所述“只有上交所锚点当天无大宗才一致”不是已证明的必要条件；也可能存在分市场字段范围差异。两次 SSE 锚点精度匹配不能证明北交所所有日总额与交易所精确相等。
5. **直接来源补强作为迁移前项目。** 尚无保留的北交所 837006 / 2023-12-04 官方日总量额直接核对，也未直接核实 600011 两个 SSE 锚点日的大宗/盘后记录。正式考虑迁移或提高数值精度/口径承诺前应补足。若取得矛盾证据，必须重新审查该业务键并恢复阻断，不能保留 PASS 掩盖冲突。本轮不为此新增市场请求或迁移。
6. **证据介质。** 297 个停牌日期由保留原始公告支持，SH600110 / 2022-10-20 为上交所 DOM 观察；该记录及北交所大宗记录没有原 HTTP 响应或截图。观察摘录的哈希不能称为交易所原始响应哈希。
7. **完整拒收计数。** 7 组历史采集共 61 次已发请求：55 次主采集、4 次复权对照、2 次拒收。拒收为最初 HTTP 500 和 USHA600289 的 `empty_points`；Claude 表格仅列后者，不能当成总数。主采集收到 46,573 点，减 702 个完全一致重叠点、186 个更早未选用点，等于 45,685 个交付观察；对照单独保留 3,912 点。两次失败响应与回执保留，均不入库。该计数不包含身份搜索和更早能力探针，各恢复计划的未发请求集合相互重叠，不能相加。

这些记录订正不改变行值、日期键、复权模式或验收阈值。北交所口径的有限证据强度、binary32 数值表示精度和上述 DOM 介质限制随交付保留，不提升为完整历史 PIT 或精确官方逐日量额承诺。

## 6. 保全、协调和后续边界

生产数据库及其 WAL/SHM/journal 共 16 个位置均按基线存在性、大小、mtime 和文件字节哈希核对，全部一致；生产 SQLite 连接数为 0。316 个既有跟踪文件对照保留基线，只有已批准的同花顺适配器、两条对应测试断言及 `.gitignore` 两个追加忽略条目发生本阶段变更。没有暂存、提交、推送、训练、账户或实盘操作。

`preservation_final_m2.json` 的旧检查器 FAIL 予以保留：旧 13 项静态清单固定了协调状态文件，恢复委派和巡检后该文件按用户要求改变。其原始字节完整保存在 `claude_resume_coordination_before.json`，SHA-256 与原清单相同：`cdd8902edff8edc11f12fc86bc081ebc943914b1a416ede8a07936b84aecab8f`。新 `closure_review/verify_preservation_closure.py` 明确核对这项授权的协调变更、限定字段和历史前缀保留；其他所有旧清单项仍使用原哈希要求。校验通过不是把旧 FAIL 改成 PASS。

Claude 报告和最终清单已稳定落盘并可重现；桌面访问当前返回 `GetCursorPos failed: 拒绝访问 (0x80070005)`，UI 文本仍显示较早的 Running 状态，未能确认会话 Idle。本次按实际文件交付及重现证据验收，**不声称观察到 Claude 会话结束**；没有再次派单、操作停止按钮或关闭客户端。15 分钟巡检曾实际启用；等待工具发生超时后恢复文件核对，不把延迟的人工巡检记为准点执行。

在此技术验收完成后，Codex 负责将现有 `claude` 巡检暂停并更新唯一协调状态。原 `v2_delivery_manifest.json` 的 `M2_complete=false` 是独立复核前的历史状态，保持原样；本最终文档与新的收口回执给出当前结论。没有自动启动 M3 或生产迁移。

回退方式：停止使用此次暂存 `CURRENT`，保留运行目录、原始体、前后回执和报告供审计；生产库未迁移，无需生产恢复。不要删除或覆写冻结证据，也不要将故意篡改过的测试候选作为回退数据。

## 7. 关键指纹与证据入口

| 文件或对象 | SHA-256 |
| --- | --- |
| `v2_delivery_manifest.json` | `eca6bea3c47ef0d37573f4b20d10d6ffe7956738a98118b7298e703142f7154e` |
| `qualification_v2_reviewed.json` | `992bd79ce9d2e38d1a0a8ae2f9890cd26daebcd3d2ab664d5caab171ec3e0f37` |
| 暂存 `CURRENT.json` | `a3d7b37b2315643970b983dedc8fa814d56219c63df350f382b9a60b0b163b35` |
| `trading.sqlite3` | `c0b26660ab903541e7e213ee312c565be73547bb3cc8c142486999717e3edeca` |
| `history.sqlite3` | `eda17434ab67496c33eed275b35045c140bad72802b4dcbf981ff22422c53003` |
| 合并候选指纹 | `d0c606fe385d54cfd9c5857864eb3e16a625fea8ee9a13e0f29de2e73eae973c` |
| 逐行记录指纹 | `5bf0da0fe7049997ce13d1f3d10c00ce142669d4c6ce33d9c82d14e385ad0832` |

新增收口证据位于 `D:\codex-A股交易\claude methods\_m2_codex_implementation_20260910\closure_review`：`reproduced/closing_audit.json`、`preservation_before_closure.json`，以及收口时生成的 `completion.json` 和 `preservation_after_closure.json`。完整采集核对为同阶段的 `v2_capture_accounting.json`。这些新增证据不覆盖原冻结清单。

Claude next-stage instruction: The M2 staging review is technically validated with the corrections recorded above. Preserve all frozen inputs, candidate stores and reports. No further M2 dispatch is needed. Await a separate user-authorized M3 or production-promotion task; do not begin either from this closure record.
