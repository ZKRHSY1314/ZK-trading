# M3-01-R2-CONTRACT-FIX-20260910

Owner: Claude, the same existing ZK-trading / Fable 5.1 project advice (fork). Codex is the only coordinator and independent reviewer. User authorization remains “开始M3吧，一样的流程”: continue M3 with real Claude implementation, Codex independent acceptance, and 15-minute inspections. This is the one next task after M3-01, not a duplicate dispatch.

## Baseline and review to read

Read AGENTS.md and CODEX_CLAUDE_COLLABORATION.md; read `claude methods/M3_01_CODEX_REVIEW_20260910.md` in full and the original M3-01 task. The review pins your completed manifest `428bb425661239de755dca6ac49e5128a923f8b90ebdb19e1c85d404e67a5147`, original module `6a0590cf2dd9b7acbc9421c818e558e24d6c2877175c8a903d47dab375a99065`, and original tests `efa2e21cb70b70fbd0c39e3e4908a7a24f9837bb45889858979f6a99f09f0ab2`. Verify the baseline before edits.

Codex re-ran your 41 tests successfully. Its 18 additional test methods have 2 passes and 16 failing methods (19 unittest failure records because one method has four failing subtests). Read the actual assertions and output under `_m3_20260910/codex/review_01_independent_tests/` and `codex/test_independent_contract.py`. These are adversarial specifications, not market observations. The review groups them into eight substantive findings, and distinguishes a proposed matching-policy correction from implementation defects. Do not claim that 19 failure records are 19 unrelated defects.

The original complete delivery, original module/tests, and the mutable coordination state at the time of your manifest are retained under `_m3_20260910/codex/review_01_input/`. The recorded old coordination hash is a historical input, not a requirement that the live coordinator file never change.

## Exact ownership and boundaries

You may modify **only**:

- `backend/app/research/m3_labels.py` (your new M3-only module; bump policy/schema version appropriately).
- `backend/tests/test_m3_labels.py` (your new M3 suite; retain substantive prior scenarios).
- Create new files under `claude methods/_m3_20260910/claude_01_r2/` only: revised written/machine policy, synthetic examples, regression map, command/test transcripts, DELIVERY.md and manifest.

Preserve `claude_01/` in full. Do not rerun its builder: it would rewrite historical artifacts using new code. The frozen originals of the two backend files live in Codex's snapshot; the new delivery must explicitly identify that those two working paths supersede their first versions. Do not edit any Codex file, current coordination/PLAN, baseline, M2 path, other source, existing label/knowledge/data, __init__, service, configuration or Git index. If unexpected new changes appear in your two paths, stop writing those paths and report ownership conflict.

Synthetic-only again. No SQLite connection, no raw market body/candidate-table read, no real-corpus feature extraction or case selection; no network, collectors, staging runs, service imports, training, accounts/credentials/funds/orders, desktop/client actions, Git stage/commit/push/PR, email, new agents or scheduler changes. You may read existing M2 metadata and qualification/event receipts as static evidence for the input contract, but must not fetch anything. Do not import app or conftest. All Python commands use project python.exe with **-B -X utf8**. Do not use py_compile (it writes despite -B); use compile(source, filename, 'exec') or AST if syntax validation is needed. No cache writes outside your new directory.

## Required correction, a coherent contract rather than isolated test patches

1. **Matching identity and counts (R1).** Duplicate records, conflicting duplicates and repeated dates of one control security must never satisfy distinct-control counts. Enforce compatible policy/version, verified record identity, universe membership and stock-vs-benchmark role, split and cutoff semantics. The initial revised policy must match on the **same decision_date, same declared intraday cutoff convention/mode, same liquidity band and same broad regime**, with 3–5 distinct other stocks. Do not silently relax this to fill a short pool; return explicit unmatched. This is a reviewed correction to the unadopted ±10-session draft. Keep deterministic tie-breaking and explicit reuse/dependence information. Full records and compact summaries must not bypass provenance checks; reject unsupported/malformed records and later-outcome fields per the declared contract.

2. **Actual review provenance (R2).** Bind each review to the immutable label/case content hash, episode id, policy and evidence version it actually considered. Reject missing evidence, mismatched case/version, unsupported reviewer kind, malformed times and conflicting duplicate review entries. A changed verdict needs an explicit superseding record/reason; preserve earlier disagreement instead of silently using the last string. Keep label core and append-only review annotations under explicit separate hash boundaries. Two supplied names cannot themselves prove two real review executions: retain external execution/evidence references and require the later coordinator to supply actual individual reviews. Synthetic demo identities/evidence must be visibly synthetic. Provide a fail-closed library counting contract: distinguish all library records, positively reviewed eligible episodes, effective dependence groups and matched controls; reject data/mode/universe/split/control deficiencies from the qualified positive count. Unknown or missing synthetic flags must not become real by default. No hardcoded positive count or claim that a helper's output proves actual review work.

3. **Complete immutable decision identity (R3).** Bind the cutoff/mode, stock data, consumed benchmark data, security/context facts, availability/provenance, prior/position state and universe/calendar/coverage inputs that affect the result. A composite id plus core hash is acceptable if every review, episode and matching consumer enforces it; a bare id must not allow a changed label/evidence version to inherit an earlier review. Exclude irrelevant future suffix rows and offered-only diagnostics from the decision core. Isolate exported policy copies and ensure the live rule configuration cannot diverge from its asserted hash. Preserve deterministic reruns.

4. **Non-price inputs and availability (R4).** Validate every supplied security fact, enum, number, date, boolean and coverage value. Bool/NaN/out-of-range coverage must be rejected; missing coverage is explicit unknown. Give non-unknown external facts evidence refs and declared availability at the relevant cutoff. Do not retrofit capture time to historical availability. A strict whole-output PIT claim must cover all actually consumed facts, not stock bars alone; absent/unknown facts remain unknown. Retrospective M2-derived labels always remain strict_pit=false, training_eligible=false. Partial known-action evidence must coexist with unknown completeness; no smooth-chart or missing-record inference of none_verified.

5. **Calendar and current-data quality (R5).** Require complete decision-day evidence before a determinate current selection; distinguish an observed suspension from a missing price and from a non-session. Use an injected, validated exchange-session mapping with a source fingerprint for real inputs; synthetic calendars must be declared fixtures. Handle interior gaps, suspensions, warmup and episode continuity using explicit session/price-observation definitions. Do not silently infer an exchange calendar from the prices offered or use natural-day approximations for qualified counts. The series API must use explicit cutoffs/conventions rather than advancing an as_of to include late data, and must not accidentally drop every strict decision bar by replacing an explicit later cutoff with 15:00.

6. **Position events (R6).** Validate chronology, source/availability and reference-price basis of position state, including reference_date not preceding the declared entry decision and explicit evidence for the reference. Known action/uncertain basis across a holding window, current missing/suspended data or unverified price comparison must yield an appropriate unknown/review/no-trade event, not a determinate stop or profit event computed from unadjusted prices. Keep signal eligibility separate from tradability/execution. Preserve label families with valid synthetic positive controls for actual well-supported invalidation/stop/exit/no-trade scenarios.

7. **Episodes and dependency (R7).** Update end selection correctly and verify order, common policy/mode/input identity, calendar continuity and state transitions. Represent candidate-eligibility changes within an accumulation phase. A minimum-duration rule must identify the first cutoff at which its duration is actually known; do not back-label the episode's start using its later end. Preserve open/censored/ambiguous/failed episodes and their evidence. Qualified effective groups must account for overlapping intervals and cooldown/dependence, including chain-connected nearby episodes, not merely start-date comparisons against one old anchor.

8. **Wire chronology protection (R8).** Use a purpose allowlist; unknown purposes must fail. Apply split/holdout checks in actual matching/review-counting admission, not only an optional unused helper. Keep the proposed development/validation/holdout intervals unchanged while implementing these fixes: development 2023-09-04..2025-03-31; validation 2025-04-01..2025-12-31; final holdout 2026-01-01..2026-09-04. No real extraction in this task. Initial real-library generation will be development-only after Codex accepts the corrected contract; validation is separate, and final holdout is not for target counting or threshold adjustment.

## Policy choices and evidence corrections

Keep the draft's cited provisional phase thresholds, windows, volume condition, liquidity thresholds and benchmark regime thresholds unless a specific internal contradiction forces a documented correction. These are observable hypotheses, not validated actor/trading truth. Do not tune them to get a target count. Unknown corporate actions may support explicitly conditional behavioural proxies, never normalized-return claims. M3-01-R2 is still not a training/paper-trading effectiveness result.

Correct the statement that the M2 evidence contains no corporate-action facts or listing dates. The frozen `_m2_ths_v2_claude_review_20260910/r06_basis_unit_controls.json` identifies nine known cash events across SH600011 and BJ920000 and points to retained documents; the qualification/pilot metadata contains listing dates. Those events are a **partial** known set with actual capture provenance, not a complete no-action register. The later reader can carry these facts read-only; do not perform a fresh data request or price analysis now. The one BJ scope interpretation remains bounded to its frozen evidence, not a general volume-validation waiver. Dates and names in synthetic demos cannot be mistaken for actual reviews.

## Validation and delivery

- Run the meaningful original 41 scenarios plus regressions for the eight groups. If an API changes, adapt your fixtures/tests honestly; do not remove a substantive scenario to keep a count or silently weaken its expectation. Do not edit Codex's test file. Add `REGRESSION_MAP.md` mapping every Codex failed method to a working new-API reproduction and its actual result, including why any old test needs API adaptation. A supported equivalently safe identity design may differ from a test's field layout; prove the outcome rather than mechanically chasing a literal field.
- Include causal suffix/mutation tests across stock **and** benchmark/context/state; same-day distinct matching, mixed-policy/version/universe/holdout failures; empty/wrong-case/stale review evidence; policy export mutation; malformed coverage/security and unavailable facts; current missing/suspension/action-safe events; overlapping episodes, effective groups, duration establishment and no held-out target inflation. Synthetic fixtures only, qualified real count zero.
- Use `D:/codex-A股交易/backend/.venv/Scripts/python.exe -B -X utf8` directly on the stdlib test script, no production conftest. Code imports must remain pure and no SQLite/network/subprocess actions. Report actual exit codes/counts/durations and retain failure transcripts from your correction work where feasible in the new directory; do not overwrite original review failures.
- Revised policy and implementation must agree. Pin the new module/tests/policy/examples/DELIVERY/regression map/test outputs and source references in a new manifest. Original `claude_01/` and M2 immutable evidence must remain byte-identical. Report exact manifest hash at ready_for_review and stop. Codex owns subsequent dispatch, policy freeze, real-case extraction authorization within M3, and independent milestone acceptance. M3 is not complete after this task.
