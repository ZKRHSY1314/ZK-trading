# M3-01-R2 工作版本预检查（不是最终交付验收）

时间：2026-09-10 07:03–07:05 UTC。Claude 的同一 R2 任务仍在执行，尚未收到完整交付清单。本页只描述下述固定代码版本的可复现结果；最终版本若已改变，必须重新核对，不得把此页当成最终退回结论或重复派单。

- 模块：`backend/app/research/m3_labels.py`，SHA-256 `975c384fb0c65f14e5265cfb140ceb49572dd7fecdcb71940f4093ba79ccc277`，版本 `0.2.0-draft`。
- 模块快照：`r2_working_preview_input_01/m3_labels.py`。原 12 项消费者检查的代码和执行程序也保留在该快照中。
- 独立数值检查：`test_feature_oracle.py`，SHA-256 `eb600ca23c310fbe242c7b745e0fe87279847f0393beee5af6c897263ea677da`，8 项通过，0.003 秒。结果在 `r2_working_preview_feature_01/`。
- 扩展消费者检查：`test_independent_r2.py`，SHA-256 `656cc5c10ac40968ca707ce78efe1e7edd562d6e5793c4f4d05eb45578c196e3`，15 项中 3 项通过、12 项失败，0.417 秒。结果在 `r2_working_preview_consumer_02/`；前一轮 12 项/9 失败记录另外保留。
- 每次测试前后模块哈希一致。实际退出码以 `execution.json.exit_code` 为准：数值检查为 0，消费者检查为 1。审计钩子禁止所有 SQLite、网络、子进程以及输出目录外写入；没有触发越界访问。

07:08 UTC 补充：Claude 将模块更新为 `5960188a809481491b31bd8faddb6021c1e5d6170eef1f58b4d935820f8d2fb0`、测试为 `6fd2aa983f6ab3d15d1f9b679af9dd35a149d22f6f7c52bb0db373a0dace0a7b`。检查差异后独立执行该版本：其 71 项测试通过（4.427 秒，`r2_working_claude_tests_01/`），8 项数值检查通过（`r2_working_feature_tests_02/`），15 项消费者检查仍有相同 12 项失败（`r2_working_consumer_tests_03/`）。这些是工作版本复现，不是已收到最终交付的证明；不同测试集的失败项数不能直接当成缺陷减少比例。

## 需要在稳定交付中复核的六组问题

| 组 | 工作版本中的具体反例 | 必须保留的结果语义 |
| --- | --- | --- |
| 审阅账本完整性 | 将 A 的合法完整账本接到 B 的合法核心，再给 B 添加审阅，可以成功；计数也未校验账本哈希、已有条目的核心绑定或执行引用。已被替代的旧条目可以再次分叉替代，且更早时间的条目可以替代更晚时间的条目。 | 读取、追加、汇总三处都验证同一个账本约束；所有旧条目绑定当前核心，哈希和时间一致；同一审阅者只有一条有效替代链，不以最后字符串覆盖分歧。 |
| 摘要证据 | 将三个实际 `candidate` 的摘要字段改成 `non_candidate`、保留原 `record_hash`，即可配成三个对照；`z` 重复 64 次也通过“64-hex”摘要检查。 | 摘要需要可解析回已经验证的核心并逐字段核对，或者限制消费者只接收完整已验证记录；哈希字符串长度不能验证摘要内容。 |
| 计数入口 | 将匹配记录的对照清空、`k_min` 改为 0、保留 `control_count=3`，仍产生一个 qualified positive；把对照改成不在池内的身份/无效哈希也可计数。同一个合成审阅记录重复输入两次，会把 independently_reviewed 从 1 增成 2。 | 固定策略决定 3–5 的门槛；计数重新验证实际匹配证据、唯一记录、对照身份、用途和版本，不信任调用方的布尔值与汇总数字。 |
| 案例持续时间 | 单个已审阅决策记录对应的 episode 明确 `meets_min_sessions=false`，计数仍给 `effective_dependence_groups=1`。 | 未证明三次连续决策达到最短持续时间的记录不能增加合格 episode 或有效组数量；逐日记录数、真实 episode 数和依赖组数分开报告。 |
| 未消费上下文 | 两份严格截点下均不可得的 ST 事实产生完全相同标签，但改变 episode_id；尚不可得的事实内容进入了 decision_inputs。 | 未消费事实保留为诊断，不改变已消费决策核心；实际被使用的上下文及其可得时间仍绑定核心。 |
| 持仓价格证据时间 | 将“某历史日收盘价”的 available_at 声明成该日期前一年，仍可生成持仓事件。 | 收盘价参考不可能在收盘之前可得；不同参考价格基础分别验证时间关系，不只检查 available_at 早于当前截点。 |

本轮通过的消费者对照是：普通合成候选构造、合法审阅追加保持核心不变、股票/基准未来价格尾部不改变当前记录。数值检查独立核对了 20/60/120 回报端点、250 根区间端点、成交量均值排除当天、成交额均值包含当天、分母为零的未知值、未来尾部隔离等。

所有价格和审阅引用均为显式构造的测试数据，未读取任何真实市场数据。三个计数边界测试专门使用“真实格式”代码、日历和 flag 值来覆盖非合成准入分支；引用仍明确以 `syn:fixture-only` 标记，生成记录只存在测试内存中。它们不是 M3 真实案例、不是实际审阅，也不能增加真实案例数量。

## 后续处理

等待原 Claude R2 任务完成并交付稳定清单。先按新清单核对模块和文件，再复跑其真实测试、这些独立反例和原始 R1 语义回归。若最终版本仍存在上述问题，形成正式验收文档后在同一协作任务中退回修正；当前不另派任务，也不启动真实案例提取。
