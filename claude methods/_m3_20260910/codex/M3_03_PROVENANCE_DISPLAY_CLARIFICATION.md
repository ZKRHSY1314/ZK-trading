# M3-03 evidence display clarification (no verdict guidance)

The accepted `m3_labels.py` at line1585 builds `provenance.source_refs` as a **sorted set union of stock and benchmark source references**. Reader source references are `trade_date#point_index` (or suspension hash locators), but the combined list is not a stock-only date range. Consequently its earliest date may precede a young stock's listing date because the benchmark has a longer history. The core separately retains stock and benchmark fingerprints and consumption counts; source-to-core replay and raw evidence binding remain accepted.

For example, the C017 SZ301529 representative has252 stock bars, while its combined reference list starts2022-08-24. That combined earliest reference cannot be called the stock's first bar or used to infer warmup. D005's feature `trade_date=2024-04-23` is the previous price date while the decision2024-04-24 has suspension evidence.

Preserve all frozen code, policy, packet/core hashes and original notes. In final provenance wording, distinguish the combined reference locators from actual stock-only range and use the accepted per-role counts, listing evidence and source-replay receipt. Add an explicit clarification/correction if an authored note calls the combined range stock-only. No need to read additional data or full chronologies. This is a display/interpretation clarification, not a request to revise any case verdict, thresholds, controls or acceptance policy. Complete the same M3-03 task and include this note as a consumed source.

Codex's own evidence extraction field named `stock_range` also misleadingly paired the first combined source reference with the maximum consumed date. Its initial method note correctly warned against using it as a stock-only start, but the suggestion that it might be a page origin was imprecise. The precise reason is the stock/benchmark union stated above. This addendum supersedes that wording while preserving the original pinned extraction, method and reviews; no authored case verdict depended on the mistaken range interpretation.
