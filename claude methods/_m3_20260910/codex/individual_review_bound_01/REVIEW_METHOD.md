# Codex actual individual review, 2026-09-10

The primary Codex agent in task `01a086db-3c02-7a21-bf22-26e4f73e24b0` displayed the pinned cutoff bundle in successive batches and authored `authored_C001_C004.json` through `authored_C029_C032.json`, then `authored_D001_D008.json`. The tool execution history records each display followed by authored judgments. Claude Message64 was independently dispatched before this review; Codex has not opened Claude's opinions. This is actual agent review, not human review. The renderer and later serializer do not decide verdicts.

Scope: all32 three-session accumulation-proxy prefixes, all127 fixed control uses and eight non-counting diagnostics. Each case is assessed from frozen cutoff features, phase precedence, contemporaneous gates, controls and counterevidence. The prior M3-02 source replay independently checked every field in all17,554 records against bounded candidate sources; this review relies on that established source/feature provenance. It does not re-read raw source prices or full later chronological sequences to form opinions. Internal core hashes alone would not be external provenance. Recent-markup and historical veto reasons are accepted on the separately pinned complete replay, not inferred from the representative bar.

The extracted evidence files retain exact fields, while compact displays round decimals only for readability. The `stock_range[0]` extraction field is an opaque first source-reference locator, sometimes a page origin, not the earliest actually consumed price date. Do not interpret its date-like text as the listing date or warmup start. Actual accepted numerical ranges/warmup are established by the source replay and per-record consumed counts; the canonical cores are unchanged. The suspended diagnostic's feature trade_date remains the prior price date even when input availability includes the decision-day suspension.

Common limitations apply to every review, including every control: non-synthetic retrospective development data; all source capture/assembly facts became available in2026, hence strict PIT=false and training_eligible=false. The fixed cohort is50 stocks with2 benchmarks, coverage refers to listed cohort keys including evidenced suspensions, not the entire A-share market or same-day tradability. Historical names, ST, float shares and turnover are unknown. Corporate actions are unknown except a partial known set for two stocks; no completeness or adjusted-return claim. Stock units are share/CNY; benchmarks use price levels and not-applicable volume/amount semantics. The one dated BJ scope exception is not a universal waiver. Calendar, context and universe are the independently pinned accepted inputs.

All current C representatives and their fixed controls are observed, development-only, stock-role, in the fixed universe, with the same decision date, close+3600s convention, retrospective mode and policy. The preceding source/packet review verified complete same-day pools/ranking; individual reasoning records each selected control's phase or veto distinction and economic limitations. Coarse matching does not control board, sector, issuer, listing age, volatility, adjustment completeness or all liquidity differences and cannot identify causal effects. Formal control-set admissibility is retained for all32; no control is removed or replaced.

Initial authored verdicts:31 positive formula-proxy episode reviews and1 ambiguous (C012). C012 is formula-compliant but is held ambiguous because its repeated rolling lows, extraordinarily low volume and possible limit-down with unknown ST leave an unresolved constrained-price-discovery explanation. This is a discretionary case judgment, not a threshold change or claim that the classifier implementation is wrong. Other positive cases explicitly retain declining-price or near-threshold counterevidence. Positive means support for the declared observable proxy; it is not a prediction of recovery, hidden actor behavior or a trade recommendation. Only C031 among these representatives has an eligible entry signal; even it has unverified execution. The eight diagnostics are separately reviewed, never counted as qualifying episodes.

All original core/packet/prefix hashes and authored opinions will be bound in serialized receipts. Shared evidence means the two agent opinions are separate executions, not statistically independent ground truth. Any disagreement remains visible; no revision will silently overwrite an original opinion. Dual consensus, dependence groups and target counts will be determined only after Claude actually finishes and Codex verifies its delivery. No live trading, production database, network/capture or M4 action is part of this review.
