# M3-01-LABEL-SPEC-20260910 — implement the isolated label contract

State: ready to dispatch. Owner of this bounded implementation: Claude, existing ZK-trading / Fable 5.1 project advice (fork) only. Independent reviewer and integration/coordination owner: Codex. User instruction: “开始M3吧，一样的流程” after M2 technical acceptance, renewing Claude collaboration and 15-minute inspections until M3 is genuinely complete.

Read AGENTS.md and CODEX_CLAUDE_COLLABORATION.md first. Read THREE_YEAR_RESEARCH_EXECUTION_GOAL.md mission, sections 2/4/6 and M3 at lines 187–208; the historical M2 ledger is not current authorization. Read M2_FINAL_ACCEPTANCE_20260910.md in full. M2 is technically validated under its approved 52-security staging scope. Codex has closed your M2 review with explicit corrections: AI readings are not human review, BJ arithmetic is conditional evidence rather than unconditional proof, SSE and BSE scopes can differ, source_name is NULL while originals survive in raw bytes, and there were two rejected history attempts. Preserve the original M2 report and every frozen artifact.

## Objective and completion for this one task

Deliver an actual pure label implementation, a written versioned policy, and meaningful adversarial tests covering all M3 label families. This is the first M3 implementation checkpoint, not the completed case library and not training readiness. Do not return another plan about preparing a plan. Finish useful implementation under the boundaries below and stop at ready_for_review for Codex.

Allowed writes, exclusively owned by Claude during this task:

- New backend/app/research/m3_labels.py.
- New backend/tests/test_m3_labels.py.
- New files under claude methods/_m3_20260910/claude_01/ only, including LABEL_POLICY.md, a machine-readable policy/schema if useful, DELIVERY.md, test output and artifact_manifest.json.

These paths must be absent before creation; if a concurrent writer appears, report it instead of overwriting. Codex owns claude methods/_m3_20260910/codex/, PLAN.md, baseline/, coordination_state.json and this task file. Do not modify them. Do not modify root goal, existing policies/labels/cases/knowledge, M2 phase directories, production source modules, services, API or frontend routes. No package __init__ changes. No Git stage/commit/push/PR. No new agents, tasks, automations or email.

Read-only source inspection is allowed for backend/app/learning/phase_replay.py, structure_scoring.py, phase_matcher.py, backend/app/agent_control/outcome_labeling.py, backend/app/strategies/dengzhan.py, backend/app/research/offhour.py and relevant existing tests/configuration/documents. Do not import or instantiate their services: some constructors initialize production SQLite and providers. Use existing legacy material only as a cited hypothesis input, never as reviewed M3 truth.

This task uses synthetic fixtures only. No SQLite connection, real-corpus feature extraction, real-case selection or new market/disclosure requests yet. M2 metadata/receipts and code may be read, but do not consume raw price bodies or candidate price tables to choose thresholds. Do not run a collector, staging runner or preservation finalizer. Policy must be fixed and independently reviewed before the later case extraction. No account/login/credentials/funds, client/service actions, training, live trading or real orders.

## Required substantive content

1. All required families: candidate / matched non-candidate / indeterminate selection; accumulation, markup, distribution, failed markup, indeterminate phase; entry eligibility, invalidation/stop event, exit event and no-trade; market-regime and liquidity context. Describe these as observable behavioral proxies, not proof of a hidden controlling actor or an actionable trade recommendation. Return review_only=true and live_trading_enabled=false.
2. Written initial namespace/version distinct from old labels. Define exact input fields, units, timestamps, required observations, deterministic comparisons, precedence and reasons. Every output must carry policy version/hash, decision cutoff, maximum input availability, source provenance references, and explicit data-quality/availability reasons. Never fabricate reviewer identity, reviewed_at or an approved status. A generator produces pending-review hypotheses.
3. Causality: consume only observations available by the decision cutoff; reject conflicting/duplicate keys, future availability, nonfinite/bool numeric values, unsorted input, invalid price/volume/amount and wrong security identity. Use an explicit cutoff rather than wall-clock date.today. Full-day daily bars are usable only after their declared availability; observation/capture time versus historical close time must remain distinguishable. The M2 corpus is strict_pit=false: a truncated calculation does not create historical point-in-time provenance. Support an explicit retrospective mode that never claims strict-PIT/training eligibility.
4. Separate any later-known outcome annotation from cutoff-time labels and matching. Never classify a past accumulation/entry using its future maximum or eventual profit. Failed-markup can be a transition known at the current cutoff after a prior observable markup, not a future-derived past label. If accepting prior state, bind it to earlier cutoffs, symbol and policy; ensure truncation and repeated runs are deterministic.
5. Unadjusted M2 prices are not automatically corporate-action-normalized features or realized returns. Specify conservative handling of known adjustment/action uncertainty, market-wide missingness, suspensions, long gaps, insufficient warmup, missing limit/IPO/float/turnover data, and the one BJ block-scope exception. Unknown is not zero and no-trade/indeterminate is an acceptable result. Do not infer no corporate action merely from a smooth chart. Do not claim execution eligibility solely from daily OHLCVA; separate signal eligibility from verified tradability and legal next-session execution.
6. Reviewable case-library contract: stable episode identifiers from symbol, cutoff, policy and input fingerprint; episode starts/ends and overlap/dependence rules; same-period, same-liquidity-band, same-broad-regime controls selected using cutoff information only; deterministic matching, 3–5 distinct controls, explicit unmatched cases/reuse counts and effective decision-date counts. At least 50 independently reviewed positive episodes is the M3 target before supervised claims go beyond exploratory, not an excuse to tune thresholds or duplicate dates. Ambiguous, failed and invalidated episodes must remain visible. Specify how two actual agents review individual cases and preserve disagreement; automatic rule agreement is not independent review.
7. Seed context: SZ002115 三维通信 and SZ002081 are absent from the frozen M2 pilot manifest. Do not fabricate their current case data or import legacy successful labels. Explain how an initial conceptual seed can be retained as legacy/unverified context while new cases come from the reviewed corpus; identify any material source/universe extension as a separate decision. Do not silently expand the 52-security universe.
8. Chronological protection: propose initial development/validation/final-holdout separation before real case extraction; distinguish it from existing production split policies, and do not consume the final test interval to choose rules/thresholds or count targets. Justify minimal provisional rule choices from specified existing hypotheses and semantics; do not optimize them against M2 prices. Document any material choice requiring Codex review before adoption. No training or performance claim in M3-01.

## Validation and delivery

Use the project interpreter D:/codex-A股交易/backend/.venv/Scripts/python.exe with -B -X utf8. Tests should exercise the new pure module directly, without the app package/service initialization or production conftest. A stdlib unittest suite loaded by file path is appropriate. No unrelated full-suite run. Include suffix-mutation/truncation invariance, invalid/missing/late data, state isolation, ambiguity, safe no-trade, matching without outcome leakage, stable IDs and no fabricated reviews, plus a constructed positive, negative and failed phase sequence. The fixtures must be visibly synthetic and excluded from real case counts.

Write DELIVERY.md with exact absolute commands, actual exit codes and counts, sources read with hashes, files changed, current limitations and concrete next step. Pin your policy, source, tests and outputs in artifact_manifest.json; output is proposed/ready_for_review, never Codex-accepted. Read-only Git status/diff checks are allowed. Preserve all pre-existing dirty files; no broad cleanup. Return the stable manifest hash and a compact substantive summary. Do not dispatch or begin the next task yourself.
