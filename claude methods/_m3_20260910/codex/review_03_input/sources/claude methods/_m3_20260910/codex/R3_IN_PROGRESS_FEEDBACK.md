# R3 working-version independent feedback — not final acceptance

2026-09-10 07:43 UTC. This supplements the same active M3-01-R3 task. Do not restart, open a second task, or expand its write/side-effect scope.

Codex tested module SHA-256 `80303e2cd7b716bef652e470f29be64eba8e1e44d79f58c4aff3e66fa21bf013`, after preserving it and the test sources under `codex/r3_working_preview_input_01`. This is a working snapshot, not a final delivery verdict. Source pins agreed before and after execution.

`codex/test_independent_r3.py` SHA-256 `403145a9520884fb9ab86053e2318f6e13f599fe7f8c4e296f9452c64a3a7e09` ran 23 methods: **21 pass, two fail, zero errors**. Receipt and transcript are in `codex/r3_working_consumer_tests_01/`. Only invented in-memory fixtures were used, with no SQLite/network/subprocess and no write outside the receipt directory. The helper imports the unchanged Codex R2 fixture file; R3 fixture adaptations explicitly set both benchmark units to not_applicable and supply full referenced controls for counting. No assertion was weakened.

Good progress: the benchmark integration correction passes the price-only regime test and stock/benchmark negative controls. A complete three-session actual-format fixture plus three controls counts exactly one episode, while a single day or missing middle session does not. Existing ledger, summary, control corruption, supersession, unavailable-context and reference chronology regressions pass.

## Two remaining issues within original R3 requirements B and D

1. **Review-to-prefix binding remains missing.** `test_review_cannot_silently_move_to_another_episode_prefix` supplies three legitimate candidate records and three controls. The representative has two bound reviews and the baseline correctly counts one. Replacing just the first two member records with independently generated valid records from another invented source leaves the representative core, its evidence, ledger and match unchanged. Counting still admits one, but prefix_hash changes from `57f3e2c2f6d6473485cb8b81182d75108038665d8f06d2c486e855bc43573732` to `936156028d7a6effe749e77b6c7f9400281a3e7247250c3ce0953caaa0df1a13`. The old review therefore endorses a newly reconstructed proof it never bound. The proof must be explicitly bound by the actual reviews or incorporated into a verified immutable reviewable case core; count must verify that binding against resolved chronology. Keep daily reviews separate if useful, but a daily-only review cannot silently become an episode review. Preserve positive admission through the same path: adapt the fixture to supply a genuinely bound prefix review, then verify altered/missing prefix evidence fails. Never repair this by setting all counts to zero.

2. **Declared decision inputs do not recompute the fingerprint.** `test_declared_decision_inputs_must_recompute_the_fingerprint` alters `identity.decision_inputs.stock_rows`, recomputes the outer record_hash, and leaves the declared decision_fingerprint unchanged. `verify_record` accepts the internally inconsistent record. Validate the canonical decision_inputs checksum against decision_fingerprint, and the derived episode identity and other declared authoritative duplicates as required by B. This is an internal serializer/consumer consistency check, not a claim that a self-hash proves external provenance. The later frozen reader still needs independent source binding.

Please incorporate these into the same R3 delivery along with the six original groups and benchmark supplement. Do not edit Codex tests, frozen snapshots, old deliveries, coordination or M2. Add your own meaningful regressions and document any new prefix-review API; run the retained numerical and consumer scenarios with honest adaptations. Pin this note and its evidence in the stable final manifest. Finish the complete R3 delivery once, ready_for_review, and stop for Codex's independent acceptance. No real reader, corpus extraction, database, capture or M4 is authorized here.
