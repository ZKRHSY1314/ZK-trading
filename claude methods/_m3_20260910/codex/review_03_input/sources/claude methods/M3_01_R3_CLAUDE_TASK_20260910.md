# M3-01-R3-CONSUMER-INTEGRITY-20260910

Owner: Claude, the same existing ZK-trading / Fable 5.1 project advice (fork). Codex coordinates and independently accepts. User authorization remains “开始M3吧，一样的流程”: actual Claude implementation, Codex acceptance, 15-minute inspections until the original M3 objective is completed. This is the single next task after the completed and reviewed R2 delivery.

## Read and verify before editing

Read AGENTS.md and CODEX_CLAUDE_COLLABORATION.md. Read `claude methods/M3_01_R2_CODEX_REVIEW_20260910.md` in full; its SHA-256 is `d417099d89931fcbeb443c6c3d3adca3c41ea43e2ee99a055b30a8546aef86a1`. Read the preceding R2 task for retained semantics/boundaries.

Codex observed your final Message 51, then froze and verified the complete R2 manifest `34b28360fc9842876a2c321d7316bec2b7f1e0b3935f1cfd17711ac1857d7451`, module `fe6046476f492aca4487492e47477166f47d1cfe052167adf021c0bf238d17e7`, tests `6fd2aa983f6ab3d15d1f9b679af9dd35a149d22f6f7c52bb0db373a0dace0a7b`, policy `e2620649cf20e2e1d80c2ddd711e6420ec2b8ea3a3d5f900c1226b1d150d37e8`. Verify these baseline paths before edits. The complete snapshot, including 13 delivery files and 16 source versions, is `claude methods/_m3_20260910/codex/review_02_input/`.

Codex independently ran your 71 tests: all pass. Its eight hand-calculated feature tests pass. Its **15 revised-API consumer tests have 3 passes and 12 failures**, grouped as R2-A through R2-F in the review. These are runtime counterexamples, not literal old-API fixture errors. Read `codex/test_independent_r2.py` (SHA `656cc5c10ac40968ca707ce78efe1e7edd562d6e5793c4f4d05eb45578c196e3`) and `codex/review_02_independent_tests/{execution.json,stderr.txt}`. Also retain the substantive 41 original scenarios, the R2 71 scenarios including their R1 adaptations, and the eight independent feature scenarios. Do not edit any Codex test or receipt.

## Exact write scope

Modify only:

- `backend/app/research/m3_labels.py`;
- `backend/tests/test_m3_labels.py`;
- create new outputs under `claude methods/_m3_20260910/claude_01_r3/`.

The two backend files supersede R2 only after their previous versions have been verified against the snapshot. Preserve `claude_01/`, `claude_01_r2/`, all Codex files, baseline, coordination/PLAN, M2 paths, other sources, existing labels/knowledge/data, configuration and Git index. Never rerun an earlier delivery builder. Codex may independently update its own files; that is not a competing writer to your two paths. Stop edits to your paths if an unexpected competing writer appears.

Synthetic-only: no SQLite connection, raw market body/candidate table access, real-corpus feature extraction, network, collectors, staging runs, service imports, training, client/desktop actions, account/credential/fund/order access, Git stage/commit/push/PR, email, additional agents or scheduler changes. No app/conftest import. Use the project Python with `-B -X utf8`; no py_compile or out-of-scope cache files. You may inspect already retained static M2 metadata as context, without fetching or changing it.

## Required coherent correction

Do not add isolated checks that only make the supplied examples green. Establish shared validation at the consumer boundaries so a deserialized record has the same meaning in review, matching, episode construction and counting. Reuse those validators; do not replace the entire label kernel or invent new infrastructure.

### A. Validate the whole review ledger whenever it is consumed

One authoritative ledger validator should verify the binding to the exact current case core, every entry's required fields/hash/case and policy binding/synthetic flag, ledger hash, and the validity of the supersession chain. Use it when loading a review status, appending a review and counting. Copying A's valid ledger onto B must fail even if B's own label core is untouched.

An entry may supersede only this reviewer's current unsuperseded entry for this case, with a reason and a genuinely later instant (compare parsed timezone-aware timestamps). Reject duplicate/conflicting entries, stale-branch replacement, cycles, unknown targets, changed entry content and malformed execution/evidence refs. Preserve the earlier dissent in the append-only history. Reconstruct status from validated entries rather than trusting cached status strings or allowing last-write-wins. Core and review hashes remain separate, and adding a valid review must leave the immutable core unchanged.

Actual external execution proof remains the coordinator's responsibility. A helper cannot establish real review work from names or invented strings; no synthetic demo can claim a human review or actual independent execution.

### B. Bind summaries to complete verified records

The compact-summary path currently lets a caller change candidate to non_candidate while retaining the old record_hash. Either require complete validated records in consumers, or add an explicit in-memory verified-core registry/resolver and compare every summary field against its resolved core. A bare summary with a 64-character string is not evidence. No SQLite or external resolver is needed.

Validate required schema/types/enums/finite quantities and canonical hash formats. Check derived identity/split/convention fields against their authoritative inputs where applicable. The hash checks detect changed content; they are not cryptographic proof that a claimed producer or source is authentic. The later reader's frozen input manifest and actual review receipts will supply that provenance. Do not silently accept unresolved or conflicting record references.

### C. Revalidate matching at counting admission

The policy fixes 3–5 distinct other stock controls. A caller cannot override k_min/k_max, control_count, unmatched, symbols or record references to pass counting. Resolve all positive and control cores, revalidate/recompute the match's same-date/mode/convention/policy/universe/role/selection/band/regime/current-state/split requirements, and compare any serialized summary with the recomputed result. Reject an absent/altered/out-of-universe control, contradictory count, wrong purpose or stale binding. When an API needs all referenced records supplied in memory, declare that requirement and fail closed if they are missing.

Deduplicate identical input records explicitly or reject them; conflicting duplicates fail. Report unique reviewed records and episodes separately from raw input length. Keep control reuse/dependence information. Later case reviews must inspect the controls as well as the positive; preserve their evidence and review status rather than allowing a match-count integer to stand in for their records.

### D. Make actual episode qualification the unit of positive admission

The current code can count one reviewed day even though its generated episode says meets_min_sessions=false. Keep the existing three-consecutive-session phase-duration rule and prove when it was first satisfied. Do not silently change it to a different new threshold merely to repair counting.

Define an immutable reviewable case/episode prefix at the qualification cutoff, with verified member evidence and identity; a bound representative decision plus a verified episode-prefix proof is acceptable. Reviewers and counting must bind the same object/proof. The prefix cannot borrow its eventual end, later eligibility or later outcome. Preserve selection changes, open/censored states, ambiguous/failed episodes and meaningful qualified examples. Do not reconstruct continuity only from a filtered list of reviewed positive days: use the complete supplied chronology for that episode, so omitted or rejected intermediate days cannot disappear.

Separate daily records, qualified reviewed positive episodes, unique controls/control uses, and effective dependence groups. Count each episode once; link overlapping/nearby intervals transitively for the dependence report. Missing/malformed episode provenance or a not-yet-established duration cannot increase either qualified positives or qualified effective groups.

Original-scope clarification: THREE_YEAR_RESEARCH_EXECUTION_GOAL.md:187–208 asks for **at least 50 independently reviewed positive episodes, with 3–5 matched controls each**, and honest limitations. It does not equate daily records with episodes or explicitly require 50 statistical dependence groups. Report both counts and define target_met transparently against the original criterion; do not hide a changed goal in one counter. Additional effective-sample limitations remain explicit, and training_eligible remains false. No tuning or real extraction to meet a target in this task.

### E. Keep offered-only, unavailable facts outside the consumed identity

Under a strict cutoff, two different future-unavailable ST declarations currently yield the same labels but different episode_ids. Store offered-only content in diagnostics, retain a deterministic effective unknown/unavailable state in the core, and bind only facts actually consumed by the decision. Apply the same principle consistently across security context, coverage, prior/position inputs and benchmark/stock suffixes where they are not consumed. All actually used facts and their evidence/availability still bind the core; do not weaken PIT checks to get stable hashes. Write and test the exact boundary.

### F. Validate reference-price evidence chronology by basis

For session_close, available_at cannot precede that reference session's close. For next_session_open, define and verify the corresponding session and earliest possible observation time, using the injected calendar and explicit evidence; do not present an arbitrary later date as the next session. For declared/unknown bases, say what is actually verified; unproved comparison basis cannot become basis_verified=true solely because an evidence string exists. Retain well-supported synthetic stop/exit/hold controls and conservative unknown/review_required outputs for missing, suspended or action-uncertain conditions. This is evidence validation, not a fill/backtest engine.

## Retain research semantics and correct documentation

Keep the four required label families, provisional phase/volume/liquidity/regime thresholds, same-date matching, development 2023-09-04…2025-03-31, validation 2025-04-01…2025-12-31 and final holdout 2026-01-01…2026-09-04. The first real reader remains development-only after independent contract acceptance. No held-out target inflation, no future-based case picking, no M4 or effectiveness claim.

M2 is 50 stocks plus two benchmarks. Two stocks have a partial known set of nine cash events; the other 48 stocks have unknown action coverage. Indexes are not extra stocks. Retain listing/warmup/PIT/price-basis limits. No new full-coverage or original-availability claims.

## Validation and final delivery

1. Exercise both rejection paths and a genuinely admissible, fully bound episode+controls fixture through the **same** counting path. Your R2 zero-count synthetic check did not test real-format admission. Purely invented, visibly fixture-only input may exercise real-format flag/code branches inside tests; do not save it as a real case or claim real review work. No test should enable a production bypass. Include a positive control so the fix cannot simply make every count zero.
2. Map all 15 methods in Codex's R2 independent file and all eight numerical methods to actual results under the new API. Honest API adaptation is allowed; do not weaken the intended conditions, misclassify real runtime failures as old-API errors, or edit Codex's files. Preserve meaningful earlier scenarios. Include transplantation, ledger corruption/branch/time, summary relabeling, fake match/threshold, duplicate record, episode-duration/identity and reference chronology regressions.
3. Run direct stdlib tests and retain actual command, exit, count and duration transcripts. Compile/AST only for syntax. Write a revised written and machine policy with appropriate schema/version changes; policy and code must agree. Regenerate only `claude_01_r3` outputs.
4. Deliver DELIVERY.md, REGRESSION_MAP.md, policy, clearly synthetic examples, transcripts and a stable manifest pinning all outputs/sources and identifying the two superseded backend versions. Keep R1/R2 and Codex snapshots/receipts unchanged. Mark ready_for_review only when all declared files exist and hashes agree, then stop. Codex independently accepts and dispatches the real reader; M3 is not complete after this task.
