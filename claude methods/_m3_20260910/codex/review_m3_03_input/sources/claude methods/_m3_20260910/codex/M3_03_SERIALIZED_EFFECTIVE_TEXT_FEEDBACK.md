# M3-03 serialization must expose corrected current wording

The current 32 reviews, 8 diagnostics, serializer and execution receipt were preserved by Codex in `codex/serialization_preview_01/` before this feedback. This working serialization is not accepted. Same task, same scope, no verdict changes.

The factual addenda were correctly authored and retained. However, `reviews/C016.json` still exposes the withdrawn "keep making new 250-session lows" claim in both `summary` and `raw_review.notes`. `serialize_reviews.py` copies the original summary directly. Consumers reading the raw review or current summary do not automatically apply the addendum. The same issue affects the other corrected C-case fields, including C031's future markup assertion. D007's effective corrected fields already show the right separation; use the same principle for cases.

Before final delivery, preserve this earlier serialization and its execution receipt/serializer by hash within your output scope. Produce current effective summary/assessment/support/counter/control text incorporating your authored addenda, with the original wording and hash-linked corrections retained explicitly as history. `raw_review.notes` must contain the effective factual summary, or explicitly point to the effective assessment without restating withdrawn claims. The report must also use the effective interpretation. Keep all verdicts and admissibility assessments unchanged. Identify the prior serialization as a superseded draft artifact with a reason; it has never been attached to a canonical input ledger, so do not create a raw supersedes reference to an absent ledger entry.

Also document that `reviewed_at` is the actual review-record sealing/binding time generated by the serializer. The earlier inspection log and authored-note history establish the real review sequence; do not describe serialization timestamps as per-case inspection start times.

Rerun the relevant binding/coverage/preservation validation once against the final effective serialization, include the retained draft and this feedback in the manifest, deliver ready_for_review, then stop. Do not open new data, change code/policy outside claude_03, manufacture another reviewer or begin M4.
