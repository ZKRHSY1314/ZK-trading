# M4-03B: bounded frozen-development replay

Task ID: M4-03B-DEVELOPMENT-REPLAY-20260912. Owner: Claude, existing ZK-trading / Fable 5.1 project advice (fork) session. Independent reviewer/coordinator: Codex. The user authorized continuing M4 with this reviewed workflow and 15-minute patrol. M4-03A delivery_02 is technically validated. Implement this bounded step once and stop ready_for_review. No M5/training.

Read AGENTS.md, CODEX_CLAUDE_COLLABORATION.md, the M4 PLAN/current_task, M4_03A_CODEX_ACCEPTANCE_20260912.md and `claude methods/_m4_20260912/historical_qualification_freeze.json` first. Preserve accepted execution, ledger, risk, M2/M3 and M4-03A evidence. This task explicitly authorizes the two frozen candidate SQLite reads below and validated outputs only in the new `claude methods/_m4_20260912/claude_03b/` folder. No production SQLite connection, dataset/knowledge mutation, network, Tonghuashun access, service changes, accounts/funds/credentials or trading/simulation clicks. Do not delegate, alter automation, stage, commit, reset or push.

## Frozen implementation contract

Read the entire accepted `claude methods/_m4_20260912/claude_03a/NEXT_READ_PROPOSAL.md`, SHA-256 `b7c9c3346341ba38b597c36d18f80f34ed0ab532168ea45d22283555731b5203`. Its revision-2 baseline, field mapping, exclusions, fixed variants and outputs are adopted subject to the explicit clarifications below. Load the accepted pure `proposal_mapping.py` by file path, SHA-256 `276cb037575f8d05a313b3266ecad46008e015d17c99ca98786c403d0c3e70de`, read-only. Load frozen m4_execution/m4_portfolio/m4_risk by file path after verifying all three freezes and policy hashes. No app package/conftest/service imports. New orchestration, guard, tests and reports stay inside claude_03b.

Before historical access: implement the guard and bounded query/reconciliation/replay runner; run focused synthetic tests for date limits, mapped timing, global event order, missing/halts, capacities and hash determinism. The already accepted mapping fixtures may be called read-only with all outputs redirected to this new folder; never execute their original main output path. Include a small two-symbol synthetic case verifying all same-session open attempts occur before that session's close decision, and shared cash/exposure and other-held-symbol marks are causal. Use injected prior-close marks for other holdings at the open; never use that session's close for risk sizing.

## Exactly allowed SQLite input paths

Both relative to D:\codex-A股交易:

- `claude methods/_m2_codex_implementation_20260910/staging_runs/ths_v2_20260910_041710_97ef9c09/run_ths_v2_20260910_041710_97ef9c09/trading.sqlite3`, SHA-256 `c0b26660ab903541e7e213ee312c565be73547bb3cc8c142486999717e3edeca`, 38969344 bytes.
- Same directory `history.sqlite3`, SHA-256 `eda17434ab67496c33eed275b35045c140bad72802b4dcbf981ff22422c53003`, 10604544 bytes.

All non-SQL sources are the exact pinned qualification bundle, metadata index, calendar, fixed 50-stock/2-index pilot CSV and prior development audit named in proposal §1 and historical_qualification_freeze.json. No new raw capture bodies or document discovery. Record hashes/stat before and after; normalize YYYYMMDD calendar dates explicitly before date comparison. Do not infer that a 500-bar adapter cap is a Tonghuashun history limit or switch sources.

Open exactly two connections for the one data-read phase, one per allowed URI with `mode=ro&immutable=1`, `uri=True`, isolation_level=None. Resolve exact absolute paths; verify SHA/size/mtime and absence of -wal/-shm/-journal before opening, after closing, and at final delivery. Set query_only=ON and verify read-back=1; disable extension loading; install a select/read-function/approved-read-pragma authorizer denying writes/DDL/ATTACH/DETACH and all unapproved operations. Audit hooks deny any other SQLite connect URI, networking, child processes, and writes outside claude_03b. Do not create synthetic SQLite databases. Guard self-check refusals must be distinguished from actual connections/validation denials. Close in finally, preserve failure receipts, never fix an input in place.

Use exactly the explicit SQL columns/predicates/parameters Q1-Q9 in proposal §2 (Q4/Q5/Q8 each on both stores), plus listed read-only guard/schema pragmas if needed. Q10 (counting later price rows) is unnecessary and omitted in this task. Every price/row-evidence/coverage/suspension SELECT must carry `trade_date <= '2025-03-31'` as a bound parameter. No SELECT *, no holdout price rows. Record SQL, parameters, row counts and permitted connection URI. Metadata tables can contain later-dated coverage descriptions; use only the declared development/warmup price keys for computation.

Read once, reconcile and seal the in-memory input snapshot; all repeated simulations use this same snapshot without reconnecting. Row equality, lineage and structural totals must match the prior frozen development audit (27900 prices each mirror, 28100 coverage keys and 200 suspension rows each, 52 qualifications/instruments, one dataset contract each and one history ingest run). Fail on unexpected schemas/counts/hashes instead of silently substituting data or widening queries. Exact schema mismatch may be reported for Codex; no speculative alternate table scans.

## Dates, strategy and causal replay

Decision sessions: 2023-09-04 through 2025-03-31 (378 frozen sessions). Warmup features: exactly the prior 250 calendar sessions 2022-08-24 through 2023-09-01; earlier halt-extended rows may only be reconciled, not used for features. The prior audit's 36 stocks with 250 valid warmup bars describes its extended input slice; recompute actual usable depths inside this task's fixed 250-calendar-session feature window rather than asserting all 36 still have 250 there. Both SMA20(D) and SMA20(previous session) need sufficient valid bars; explicitly count insufficient history. No holdout/validation price access.

Baseline B0 and parameters are fixed before the read exactly as proposal §4: SMA20 cross, initial hypothetical cash 1000000, position cap0.05, gross cap0.60, stop0.05, profit target0.10, max holding20, cooldown5, max mark age0, gap0.02, expiry1, fixed hypothetical fee/lot/tick/T+1/slippage. The first five listing sessions are excluded from entry; document listing-age indexing. Never tune to obtain trades or select winners. A zero result must remain zero with causes.

Run these separate predeclared branches: assumed full_fill primary, assumed fixed_5000 sensitivity, assumed capacity-none sensitivity, and raw evidence eligibility/refusal path. Participation=1.0. Each branch starts from a fresh independent ledger. For each branch repeat the complete run twice from identical sealed inputs and compare normalized output and event hashes; runtime timestamps belong only in receipts. One symbol-session open capacity ID must retain the same declared budget across uses, with ledger-controlled cumulative consumption. Log actual downsizing/rejections; do not refresh capacity under a new ID to force fills.

At each session, process due open attempts in deterministic exit-before-entry then symbol/intent order, with their original bound decision contexts. Then mark/decide at 16:00 using closes available by that time. All session chronology is global, not one stock's whole history followed by another. At open other held symbols use the last already-available close, with actual dates/age and provenance. Stale/unknown valuation remains incomplete, not zero. Confirmed full-day halts have no print; the accepted suspended placeholder may be used solely to obtain a refusal, never as a tradable price or close mark. Missing unconfirmed rows do not become fabricated halts.

Raw path must preserve capture availability and actual refusal causes; its expected zero intents is not produced by secretly injecting assumptions. If raw baseline calculation is shown as a retrospective diagnostic, label it separately and do not treat those values as available raw decisions. Never invoke a raw attempt with assumed capacity. For all assumed branches retain raw source provenance structurally (symbol/date/row evidence and raw capture timestamp) and separately record model time and assumption IDs in every exported wrapper, including decisions, attempts, benchmark/performance and errors. Do not relabel model timestamps as source timestamps. Even if engine records omit a global assumed flag, the enclosing research record must make the grade explicit and retain the exact unmodified engine record.

No close/high/low/volume from the attempted session can influence its open state, budget, fills or earlier decisions. Volume diagnostics and known ex-date flags are strictly post hoc and never change historical records. All positions carry adjustment_uncertainty=true. The fixed pool's selection bias and missing historical ST/bands/corporate-actions/delist/fee provenance remain explicit.

Calendar metadata may include the next legal session needed to derive expiry/T+1 at the end; no prices from that next session may be read. On 2025-03-31, report still-open positions and beyond-window intents as censored/unexecuted with their actual engine states; do not fabricate a forced liquidation, successful expiry event, zero terminal value, or a close made from 2025-04-01 prices. Last-close valuation is hypothetical and may be incomplete when a holding has no eligible mark. Benchmark SH000300 endpoints and availability must align with the reported period.

## Output and validation

Implement the runner, actual guarded read, reconciliation, four branches/two repeats, per-stage funnel and honest REPORT.md. Outputs only under claude_03b, with stable input/model hashes, manifest last, and no overwrite of previous runs (use separate failure/diagnostic receipts). Save source and script hashes, pre/post file states, all executed commands/exit codes, connection/SQL audit, per-symbol exclusion reasons, signal/intents/attempts/fills/exits counts and all event/ledger records needed for independent replay. Reconcile cash, positions, FIFO cost, PnL, capacity and reservations; sum funnels with mutually exclusive counts or clearly document overlaps instead of double counting.

Re-run the mapped future-suffix control using synthetic bars inside this task's runner so changes to later OHLCV cannot affect an earlier open record. Preserve raw input bytes and original approved modules; independently hash-check execution/portfolio/risk/historical-qualification freezes plus 316 tracked/42 historical immutable/16 production baseline entries. PLAN and coordination_state are Codex-owned mutable records, not immutable input pins; record historical reads but don't fail final preservation merely because patrol updated them.

Report three distinct outcomes: already validated synthetic engine evidence; this actual assumed historical-shape replay with observed counts and conditional PnL; unmet genuine historical execution evidence. M4_complete=false, strict_pit=false, training_eligible=false, review_only=true, live_trading_enabled=false, M5_started=false. No self-acceptance. Do not interpret assumed nonzero fills or successful guards as historical eligibility passing. Stop after the final artifact_manifest.json for Codex independent acceptance and M4 requirement reconciliation.
