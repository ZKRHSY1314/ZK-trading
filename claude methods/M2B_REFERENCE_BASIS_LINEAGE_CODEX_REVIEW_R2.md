# Reference-basis lineage revision 2 — review and next bounded task

2026-09-09. Reviewed hash `4b69ba631f587070b6c2a72f90ea92af5ad06bb60421cd8766268c9249a46157`. **The substantive corrections are largely resolved; two residual statements prevent unconditional closure of RL-R1/RL-R2.** Preserve the corrected trace and do not reopen resolved arguments. Fold these two edits into the next useful static analysis rather than spending another round on this document alone.

Codex compared the entire revision diff, checked the eight source hashes and 91 protected pins, and checked all 46 G1 files against the frozen delivery: all match. The seven stock and four index grouping rows are unchanged from the independently checked prior revision. No numerical study or production tests rerun; no database, network, provider or service invoked. HEAD `73f266d`, staging empty. Frozen reviewed bytes and verification: `_m2_codex_review/reference_basis_lineage_review_20260909_r2/`.

## Remaining scoped edits

1. **RL-R2, section 10 lines 571–572:** “A large gap between the two therefore indicates an ordinary re-write, not a migration.” The valid inference is that an ordinary rewrite **can produce** such a gap; the observation identifies neither that rewrite nor migration execution and cannot rule out both. Replace this sentence accordingly. The surrounding description of the current SQL and the refusal to infer migration from timestamps are sound.
2. **RL-R1, section 10 lines 595–597:** “not something any read of existing local files can supply.” This still exceeds the inspected-file scope. Say that the needed historically bound evidence is absent from the inspected artifacts, cannot be reconstructed from the selected row fields alone, and must not be presumed absent from every uninspected local file. The main headline, matrix and response/selection qualifications are otherwise adequately corrected.

No other rewrite of the lineage audit is requested, and no authorization receipt is needed. A different phrasing is acceptable if it carries these exact inferential limits. Existing technical acceptance, policy and evidence boundaries are unchanged.

## Next assignment: CACHE-WRITE-PATHS-20260909

**Authorized write scope:** (1) make only the two scoped corrections above in `claude methods/M2B_REFERENCE_BASIS_LINEAGE_AUDIT.md`; (2) create `claude methods/M2B_CACHE_WRITE_PATH_AUDIT.md`. No other files, scratch scripts or output directories. This task continues the user's existing offline-analysis authorization.

**Goal:** map actual current paths that can insert, replace, update or delete `daily_bar_cache` rows, and show whether the per-upsert `ready`/`qfq` guard protects those paths. This bounds the current-code inventory left open by the lineage audit; it does not establish which path ran on historical reference rows.

- Search `backend/app` and `backend/scripts` source as text, plus directly referenced local schema/model definitions. Record the search scope and actual search patterns. Follow dynamic SQL/table-list or indirect service calls relevant to this table; a mere `adjustment_mode` mention is not a writer. Distinguish direct SQL, indirect refresh callers, read-only consumers and unrelated tables. Do not execute/import these modules.
- Include `daily_bar_cache.py`'s `_upsert_bar`, `_upsert_bars`, deletion-before-upsert branches and cleanup/error paths; `sqlite_store.py`'s DDL and migration updates; and `backend/scripts/import_legacy_data.py`. Codex found a direct `INSERT OR REPLACE INTO daily_bar_cache` at lines 630–647 in a **demo-seed** branch, with `demo_seed_fixture` source and `demo_fixture` quality, and without adjustment/unit columns. Read its actual activation conditions and generic table-import machinery rather than assuming it imports real reference rows. Never run the importer or open its input datasets.
- Trace `universe_backfill.py`, `snapshot_builder.py`, `learning/phase_replay.py` and `market_history.py` only to classify actual caller/reader/writer roles. Avoid confusing the separate `daily_bars`/history tables with this cache. Where broad/dynamic importing makes completeness unprovable, name that limit rather than claiming every possible writer is enumerated.
- For each actual writer/deleter, state trigger/caller, SQL operation, columns supplied/defaulted/preserved, timestamp convention, guard applied or bypassed, and whether an ingestion/migration event is durably recorded by that path. A deletion followed by insertion is not necessarily protected by an `ON CONFLICT` guard: assess the actual sequencing and predicates. Explain consequences conditionally without claiming an overwrite or defect happened in retained data.
- A concise operation/guard matrix and a few exact source snippets are sufficient. Hash every source file relied upon and note relevant uncommitted differences without modifying them. Preserve prior source hashes and all retained evidence. Report what this inventory adds to L-5/L-6 and what it still cannot establish.
- End with any **concrete useful remaining existing-file evidence task**, if there is one. Do not invent another documentation rewrite or fit more price-relation families merely to prolong work. If a real missing historical link needs outside data or database access, identify the evidence type and why it would discriminate; do not request or use that access.

No SQLite opens (including in-memory SQLite), imports initializing providers/services, migration execution, legacy import, network/retrieval/capture/replay/decoder, production code changes, dataset/strategy/knowledge mutation, policy/gate/label/eligibility changes, threshold calibration, pilot/backfill/training, source expansion, Git staging/commit/push or live trading. Do not read credentials, tokens or raw legacy input datasets. Static source analysis and retained-document reads only; no tests or new numerical study needed.

Deliver both resulting document hashes, concise findings and preservation verification at `proposed for review`. P1 open, U-6 deferred, every eligibility false, source capability FAIL including retained EV6; both capture authorizations consumed. This is a normal handoff inside the ongoing workflow, not a pause or a new permission request.
