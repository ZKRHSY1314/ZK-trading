# M4-03A correction matrix — Codex review 01 (delivery 02)

Review: `codex/M4_03A_REVIEW_01.md` (sha256 `394ab7f9ba63ba9237cb2d45490c5da1f02920002781164d271b335938515161`); Codex probes `codex/probe_qualification_proposal_01.py` / `_02.py` with receipts under `codex/qualification_review_01/` (`independent_proposal_probes.json`, `independent_proposal_probes_02.json`, `verification.json`, `replay_receipt.json`). Codex files were not modified or re-run. The matrix, 24 pins, 65 anchors, 9 kernel cases, 5 classifier cases and the preservation checks passed and are retained unchanged in substance (378 sessions / 17 402 rows / 152 halt keys / strict historical baseline **not provable now**; `strict_pit=false`, `training_eligible=false`, M3 unchanged). Delivery 01 is preserved verbatim under `superseded/delivery_01/` (manifest `0b680bd5…`, `DELIVERY_STATUS.json`).

What changed: the M4-03B proposal (`NEXT_READ_PROPOSAL.md` §4, §6, §7), a new pure mapping module `proposal_mapping.py` that implements the corrected evidence mapping, ten full-chain synthetic checks in `validate_qualification.py` (frozen risk → ledger → kernel → exit, hash-verified engines loaded by file path, no app/conftest), two wording fixes in `qualification.json` rows R04/R05 (limit state from the open print; tradability from the halt ledger + open-print assumption, never from the later existence of a bar), this matrix and the manifest. No accepted module, freeze, historical evidence, Codex file, PLAN or coordination state was touched; no SQL, network or historical trade.

| # | Finding (Codex) | Reproduced | Root cause in delivery 01 | Correction in delivery 02 | Verification (all in `evidence/validation_receipt.json:full_chain_cases`, exit 0) | Status |
|---|---|---|---|---|---|---|
| P1-1 | `TradabilityEvidence.limit_state` at the 09:30 attempt was derived from `close(D+1)` (a future value) while marked available at 09:25; the frozen kernel turned `filled` into `unfilled` when only the future close changed | yes — Codex `future_close_dependency`: same open, future close 10 vs 11 → `filled` vs `unfilled: limit_up_no_buy_fill` | the proposal table mapped `limit_state` to `close(D+1)` vs the band; no structural barrier kept later fields out of the attempt | `proposal_mapping.OpenAttemptInputs` accepts only `open(D+1)`, `close(D)`, the halt key, capture instant, board and listing age — no high/low/close/volume field exists; `limit_state = limit_state_from_open(open, band(close(D)))` (`limit_up` if open ≥ hi, `limit_down` if open ≤ lo, else `none`); later fields feed only post-hoc diagnostics; known ex-dates became an explicit **post-hoc performance flag** (`ex_date_flags`) that never removes a decision or alters the ledger (original availability of the notices is not established) | `future_suffix_invariance`: bars B change 03-19 high/low/close/volume (close 11.00 = band top, volume ×5) and later bars → D1 and X1 record hashes, kernel `result_hash`, tradability state (`none`), fill 400 @ 10.08 identical; D2 diverges only after the 03-19 close becomes available (equity 100 081.92 vs 100 361.92). `limit_state_from_open_only`: open 11.00 → `limit_up` → buy `unfilled: limit_up_no_buy_fill` (intent expired); open 9.00 → `limit_down` → buy fills 400. `limit_state_pure` + `assumed_band` (BSE 10.00 → 7.00 / 13.00). `ex_date_post_hoc_flag`: flag set only when an ex-date lies inside the position window; PnL and records unchanged | fixed |
| P1-2 | price time semantics and the STRICT / ASSUMED paths were not a runnable, causal contract: §7 demanded "no `available_at` before the bar's own close" (impossible for the 09:30 open print); the frozen risk layer refuses `price.available_at > executed_at` even for `predeclared_assumption` (`m4_risk.py:639`), so the delivery-01 validator (kernel only) did not prove the full chain; STRICT was described as the assumed chain with capacity removed | yes — Codex `risk_availability_mapping`: raw capture `available_at` → `future_evidence` at the risk layer; assumed 09:30 → filled | the mapping conflated raw capture time with model availability and had one field for both; no full-chain test existed | `proposal_mapping.py` keeps them apart per field: the raw capture instant is written verbatim into every `source_ref` (`captured=…`, never overwritten); model instants are declared per field with assumption ids (`close` fields 15:00 → 16:00 of their own session; the `open` field at its own 09:30; calendar 2022-08-24); `variant="raw"` uses the raw instants everywhere and declares nothing; `variant="assumed"` labels everything. Strict-raw is a separate path (refused at the decision stage), capacity removal is a sensitivity of the assumed chain, and the impossible blanket rule was withdrawn (§4.1, §7) | `assumed_full_chain_smoke` (hand-calculated): 400 shares (budget 5 000.00, est. 4 014.08, limit 10.20) → fill 10.08, cash 95 961.92, `evidence_grade=assumed` in the ledger's kernel record and in a pre-execution dry run whose `result_hash` equals the ledger's, with `assumptions_used` = price / capacity / st_status / listing_state ids and `captured=` in `source_ref` → hold (equity 100 081.92, ref 10.0952) → stop at 9.50 (limit 9.31) → sell fill 9.38, cash 99 704.84, realized −295.16, cooldown 03-21 → 4× `cooldown_active` → re-entry 400 (budget 4 985.24) → performance −0.002952 vs benchmark −0.010000, ledger reconciles. `strict_raw_decision_refusal`: raw instants → `future_evidence` for mark, benchmark and signal at the decision; 0 entries, 0 intents, 0 ledger records. `raw_attempt_after_assumed_decision`: raw attempt evidence → `future_evidence`, intent and 5 000.00 reservation untouched, no ledger record. `assumed_capacity_removed_sensitivity`: `capacity_unproven`, ledger `no_effect`, intent `expired`, cash 100 000.00, reservation released. `halt_session_placeholder_cannot_fill`: `suspended`, placeholder price field, no position | fixed |
| P2-3 | capacity `quantity = order quantity` with `max_participation_rate 0.10` gives `allowance = floor(q × 0.10)` = 10 / 40 → `capacity_below_minimum_lot`, contradicting "full fill" | yes — Codex `capacity_full_fill_mismatch`: 100 → allowance 10, 400 → 40, both unfilled | the participation haircut was left at 0.10 while the capacity was already an assumption | `max_participation_rate = 1.0` in every variant; two pre-fixed variants independent of daily volume: `full_fill` (capacity = order quantity) and `fixed_5000` (5 000 shares per open attempt), plus `none` as the sensitivity; parameters are fixed now and not changed after results | `capacity_model_consistency` (kernel, `evidence_grade=assumed`): full_fill 100 → allowance 100, filled 100; 400 → 400; same `capacity_id` with `consumed_quantity=400` → `capacity_exhausted`; fixed_5000: 400 → filled 400 (allowance 5 000, consumed 400); 9 900 → partially filled 5 000, remaining 4 900 | fixed |

## Retained and restated

* The qualification verdicts are unchanged: 4 present / 3 assumption-only / 5 missing; strict historical execution eligibility 0 of 12; a nonzero genuine historical baseline is not provable now; the M4 item "on eligible historical data" remains unmet. Nothing in this revision upgrades an assumption to evidence.
* Every replay record is either fully raw (and refused by the frozen engines) or fully labelled (`assumed`); there is no partially labelled state. The proposal remains a proposal for Codex review; M4-03B was not started.
* Codex's own probe fixture (first version `max_gap=0.5`, second `0.02`) needed no Claude change; both Codex receipts are read-only inputs here.

## Evidence

* `evidence/validation_receipt.json` — guarded run: 24 pins, 12 rows, 65 anchors, recomputed numbers, safety flags, 9 kernel cases, 5 classifier cases, **10 full-chain cases** (proposal_mapping sha256 recorded; kernel/ledger/risk policy hashes `9bea8348…` / `633f78d9…` / `013f1580…`), guard self-check 5/5 denied, 0 denials during validation, `app` not imported.
* `evidence/prework_verification.json` / `postwork_verification.json` — three freezes 49/74/82, 42 immutable pins, 316 tracked files, 16 production positions, 14 evidence pins, both databases by hash/stat only.
* `superseded/delivery_01/` — delivery 01 byte-preserved with `DELIVERY_STATUS.json`.
