# M4-02B 第一轮独立复核：需修复

结论：17 项交付测试通过，但 Codex 独立合成检查 5 项仅正常入场通过，4 项真实反例失败。暂不进入 M4-03。

被测源码 SHA-256：`a4466f37bf5c1da8cae70c68b27e843cbd7aaa3f5ce0136bb14dacb377e0f174`。
测试 SHA-256：`bcd9d2275650f88d2b835cd46b4b6d2aa96df4eccb8635add2f3ceedbd6e32ea`。
交付清单 SHA-256：`f1d1979cbec72b694b9e4d9aad33b360684d475e03d3d92231aa43ab150a43c1`。

证据：`codex/probe_risk_01.py` 使用 Codex 既有独立执行夹具和新风险层入参，不调用 Claude 测试夹具；源码快照和结果在 `codex/risk_review_01/`。17 项原测试另行隔离运行。17 项交付文件、21 项来源、49+74 项两级冻结、316 项原跟踪文件、42 项历史冻结和16 项生产文件位置全部符合基线。没有 app/conftest 导入、SQL、网络或生产写入。探针开发期间一次字段名误拼已纠正后运行，不属于产品失败。

## Required corrections, same M4-02B scope

1. **P1 — cumulative partial fills reuse the full reservation (`execute`, lines 535–589).** Starting cash 10000 with position/gross caps 50% reserves 5000 for 400 shares. First fill 100 @10 costs1005.01. A second fill 300 @14 costs4205.04 and is accepted because it is compared against the original5000 again. Total acquisition cash is5210.05, exceeding the original budget. Debit actual applied cost, including per-attempt fees, from the same intent budget and expose original/consumed/remaining amounts. Do not reuse released or consumed resources for another pending intent. Recheck current permissible exposure/cash using causally eligible valuation inputs before application; a stale original budget alone is not a current portfolio risk check. If marks required to do this are missing, refuse explicitly. Cover different-price partial fills, same-price repeated minimum commissions, multiple symbols/reservations, reduced remainders and exact boundaries. Preserve the positive 400-share single fill (cash5994.96).

2. **P1 — invalid/future evidence mutates intent state before validation (`execute`, early gap path).** A valid pending buy with a5000 reservation is cancelled and releases the reservation when supplied price100 observed/available on March20 is used for an attempt on March19. The early affordability path returns before the frozen kernel validates evidence. Validate all consumed execution evidence and legal temporal/symbol/unit/phase context before cancellation, downsizing, reservations or any other state transition; invalid/mismatched/future inputs must be an atomic refusal. Add exact future-price cancellation regression, wrong-symbol price, invalid phase and earlier-than-eligible attempts, with both zero-affordability and normal paths. A valid contemporaneous unaffordable gap may still cancel under the declared contract.

3. **P1 — original decision context is not bound through first execution (`_new_intent`, `execute`).** A March18-close buy intent has an original March19 15:00 expiry. Before its FIRST ledger fill, changing only the supplied execution context calendar.close_time from15:00 to16:00 allows a March19 15:30 fill. The ledger cannot detect this because it first registers that rewritten calendar. Bind the causal context and temporal contract at intent creation (calendar facts needed for eligibility/expiry; declared phase and fee/assumption semantics as consumed). Enforce them before first and later execution; do not let `evidence.phase` redefine order.execution_phase without a declared order contract. Preserve irrelevant future-suffix freedom. Handle a genuinely expired intent deterministically without keeping an impossible pending reservation forever, while malformed/future requests remain refusals rather than expiry events. Test first-fill after-original-expiry mutation, original calendar expiry, valid within-window attempts, relevant policy/context changes, and expiry/re-evaluation/reservation release with no fabricated fills. Both frozen modules remain read-only.

4. **P1 — decisions consume ledger state from the future (`decide`, chronology check).** After a March19 10:00 applied buy, a new decision id at March18 16:00 is accepted and values the March19 holdings using the March18 mark. `last_decided_at` alone is insufficient; it equals the original decision time while the ledger has advanced. Refuse decisions/valuation snapshots earlier than the ledger state they consume (and audit the relevant event chronology), or implement an explicit immutable historical snapshot mechanism within scope. Apply the boundary consistently to performance/valuation/cancel paths as appropriate; no silent use of later holdings/cash for earlier timestamps. Add the exact backdated decision, performance-as-of before last fill, same-instant ordering and valid next decision controls. Retain earlier records unchanged.

These are correctness issues in the requested current scope, not a request to expand strategy features or alter synthetic policy to improve results. Review the related paths holistically so a fix does not merely mask each literal fixture.

## Next instructions to Claude

Continue SAME task M4-02B-RISK-EXIT-20260912 under its original exclusive write scope. Preserve delivery_01 by byte/hash before replacements. Read this report, reproduce all four independent failures from the saved snapshots/results, and implement the underlying contracts; do not modify Codex evidence or either frozen module. Replay probes only into your own evidence directory with unchanged semantic expectations (document any necessary API adaptation). Add meaningful regressions and hand arithmetic, run guarded isolated final tests, update CONTRACT/HAND_CALCULATED_BASELINE and a correction matrix, verify preservation, then write the complete manifest last. Report ready_for_review with final hashes and stop. No M4-03/M5, historical data access, other agents or automation changes.
