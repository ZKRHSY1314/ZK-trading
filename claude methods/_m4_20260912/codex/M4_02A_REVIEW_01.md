# M4-02A 第一轮独立复核：需修复

结论：暂不通过，不能进入 M4-02B。Claude 自测报告 25 项通过；Codex 独立合成探针实际运行 5 项，正常往返手算通过，另外 4 项复现真实阻断问题。源码在探针前后未变，使用了保存的字节快照；没有修改 Claude 文件或冻结 M4-01。

被测账本 SHA-256：`96fcd3a1858670c44e70623141e867438cc150eea990978d3635fd11377f416b`。
冻结执行内核 SHA-256：`83a28b543b9ecc5edf8080ea39fa388f8b2bb5a8724ef68b4b532041216678c7`。

复现：`codex/probe_portfolio_01.py`，快照和逐项原始输出 `codex/portfolio_review_01/`。正常买卖 100 股后现金 10089.43、已实现盈亏 89.43，核对正确。以下是额外边界，不依赖 Claude 的测试夹具。

## Required corrections in the same M4-02A scope

1. **P1 — cross-symbol settlement leakage (`account_state`, `_apply`, `_apply_fill`).** `account_state()` pools every symbol's lots into the kernel's symbol-less InventoryLot tuple. A ledger with 100 target shares acquired TODAY and 100 OTHER shares acquired previously can sell the target today: the kernel counts the other symbol's settled quantity, then the ledger disposes the target's unsettled lot. This bypasses T+1. Derive symbol-specific inventory for the requested security, while retaining shared portfolio cash; enforce the same symbol and settlement selection when allocating FIFO. Adapt the new ledger interface explicitly, without modifying the frozen execution kernel. Add cross-symbol tests with mixed settled/unsettled inventory, missing target inventory, distinct symbols on the same acquisition session, and odd-lot rules. The original same-symbol positive control must remain valid.

2. **P1 — rejected attempts are not atomic (`_apply` commit sequence and `_apply_fill`).** Selling a symbol with no position while another symbol supplies the kernel's pooled inventory leads to `invariant_violation`, but capacity is already consumed and event/attempt/sequence registries already changed. Replaying the rejected event becomes `skipped_duplicate`. The result claims rejection and unchanged state_hash while executable state has changed. Do all validation before commitment or stage changes transactionally and commit once. Every ledger-level rejection must preserve cash, lots, capacities, orders, dedup registries, sequence/times and state hash; an append-only rejection audit entry may be recorded separately. Also prove atomicity for result-integrity failures after early validation, including correctly rehashed but inconsistent cash_after/capacity fields, and allow a corrected event to reuse an uncommitted identity. Deep-copy nested existing order attempt lists if staging; `dict(order)` currently aliases them.

3. **P1 — public read results alias stored state (`apply`, `snapshot`, `_state_body`, records view).** Editing the dict returned by `apply()` changes `ledger.records[0]` because the same object is stored and returned. Editing `snapshot()['capacity']['C']['evidence']['source_ref']` mutates the live evidence registry because snapshots copy only one level. The state/event hashes then misrepresent the content, undermining provenance and earlier-prefix immutability. Return detached deep copies or immutable views, including nested lot provenance/capacity evidence and exposed record collections. Regression tests must mutate returned nested values and verify internal snapshots, prior audit records, hashes, future budget checks and reconciliation remain unchanged.

4. **P1 — same order identity can rewrite its causal/expiry terms (`_apply` order tracking).** A partial continuous order expires March 19 at 15:00. At March 20 10:00, a new attempt using the same order_id and decision_id but a replaced decision session, submitted_at and eligible_from fills the remaining shares; the original expiry was silently moved. The registry checks only symbol/side/decision_id and quantity, not the actual original order/decision terms. Bind immutable decision/order terms at first accepted registration and enforce expiry from that original contract. Attempt-time evidence may change; quantity reductions may follow the declared rule; changing limit/phase/eligibility/expiry/decision content requires an explicitly modeled amendment or a new order identity. Add post-expiry retries, changed phase/limit/decision payload, permitted quantity reduction, and valid same-order within-window attempts.

No frozen M4-01 change is required by these counterexamples. No relaxation of T+1, identity semantics, review-only status, preservation or the task scope is authorized.

## Next instructions to Claude

Continue **M4-02A-PORTFOLIO-LEDGER-20260912**, using the existing exclusive write scope only. Read this report, reproduce all four failures from the frozen raw results, preserve your first delivery by hash under claude_02a before replacing it, and fix the underlying state contracts. Do not edit Codex probes/evidence or rerun them into their frozen directories; replay them to your own evidence with unchanged semantic expectations. Add meaningful regressions, run the guarded isolated suite on final bytes and rerun the frozen kernel suite read-only if needed. Provide a correction matrix, actual execution/preservation receipts and a complete manifest written last, then report ready_for_review with SHA and stop. No M4-02B, historical data access, other agents, training, or automation changes.
