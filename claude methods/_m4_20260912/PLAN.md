# M4 回测引擎有效性

状态：M4-01、M4-02A、M4-02B、M4-03A、M4-03B全部完成限定工程技术验收，五份冻结已保存。原始M4研究验收仍因历史可用性、幸存者偏差与合格历史非零基线证据不足而未达标；M4_technical_complete=true，M4_complete=false。最终报告与机器回执已验证，按既定结束条件暂停巡检；不启动M5/训练。

M3 保持原样：工程验收已收口，双审正例0/50、32争议，M3_complete=false、strict_pit=false、training_eligible=false。用户本次明确启动 M4，允许进入引擎验证，但不意味着数据或监督研究已达标。

1. M4-01：静态复核旧引擎，实现新的纯执行内核、显式时序/费用/可成交证据契约及合成对抗测试。代码只写 m4_execution.py 和对应测试；不连接数据库、客户端或网络。
2. M4-02：先 M4-02A 隔离组合/FIFO现金账本、事件去重、累计容量和非零手算基线；验收后 M4-02B 实现风险规模/最大暴露、止损/退出/冷却、基准、退市。均要求时间后缀不变性，具体写入范围由相应任务书规定。
3. M4-03：先静态查清现有冻结开发数据对执行验证的资格，必要时在下一任务书明确允许固定候选来源只读查询或读取已冻结文件。不得默认未知ST、复权或成交容量已知。分开报告合成引擎证明、显式假设下的历史重放和真实历史执行证据；完整保留来源、逐级排除与非零基线或无法证明的原因。不得挑选结果、消耗留出期或扩大总体来凑交易。
4. 最终对照原始 M4 六项验收要求逐项说明满足范围、测试和历史证据。未证明的要求不得写通过。最终文档与机器回执先落盘，再暂停原巡检；不自动启动M5。

所有旧数据、源文件、M2/M3证据与协调状态只读。实盘关闭，不新增账户、凭据、资金、下单/撤单、屏幕模拟点击或服务变更。Codex 独占派单、集成、协调状态与巡检；同一时刻只有一个 Claude 写入任务，运行或待发送时不重复派单。历史规则/费率仅在有来源审核后用于真实口径，目前只允许带明确假设标签的合成费率。

当前任务以 coordination_state.json 的 current_task 为准。等待实际稳定交付及原生界面完成状态，不用“文件出现”或“测试通过”代替任务完成或历史有效性。

