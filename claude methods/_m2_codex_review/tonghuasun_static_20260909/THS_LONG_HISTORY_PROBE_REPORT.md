# 同花顺长历史隔离验证：认证阶段停止

日期：2026-09-10，Asia/Taipei。负责人：当前 Codex；独立复核与合成测试由 Codex 子代理完成，未向 Claude 派单。

## 结论

**第 1 次本机 REST 请求收到 HTTP 401，验证器立即停止；6 次上限已消耗 1 次，其余 5 次未发出。没有取得 K 线，不能据此判断长历史能力。**

只读排查发现当前进程与项目配置目录不一致：当前 `happ.exe` 的 PID 是 **29876**，匹配用户目录中的运行时端点；项目指定的 `D:\TonghuasunCodex` 端点仍是 2026-09-04 的 **PID 23512**。本次请求使用项目目录的配置，而非当前进程对应的用户目录。这是明确的配置身份不匹配证据；没有读取或比较另一目录的令牌，也没有证明进程内实际令牌值或该差异的全部因果机制。

用户已经确认行情登录。401 属于插件本地 REST 鉴权错误，**不能解释为用户未登录、行情为空、同花顺不支持超过 500 根，或同花顺数据源能力 FAIL**。此前静态复核的正向证据仍成立：项目 500 根截断不能充当底层限制；真实长历史覆盖仍未观测。

## 本轮范围与实际结果

用户确认的范围是编写隔离验证程序，对 SH600011、BJ920000、SH000300 开展最多 6 次行情只读请求，仅在本审查目录留存证据，不修改生产代码或数据库。

固定方案：每只证券依次发送 count/range 两种请求；`limit=1200`、日线 `period=7`、请求不复权 `adjustment=0`。显式证券为 `600011.SH`、`920000.BJ`、`000300.SH`，没有调用生产代码中错误的指数转换函数。

| 项目 | 记录 |
|---|---|
| 本轮标识 | `M2-THS-SIX-READS-20260910` |
| 唯一实际请求 | POST `http://127.0.0.1:17180/api/v2/quotes/candle`；SH600011 count |
| 请求保留时间 | 2026-09-10 00:26:33.673888 +08:00 |
| 参数窗口 | start=null；end=`2026-09-04T15:59:59.999Z`，即中国时区 9 月 4 日结束 |
| 结果 | HTTP 401；`ok=false`、`data=null`、`error.code=unauthorized` |
| 耗时 | 0.031 秒，收据测量值 |
| 原始响应 | 240 字节，完整保留，没有 K 线内容 |
| 原始响应 SHA-256 | `aa8f9bb033ca3a80f529ccf4a57a60c474ffa4935b7f8b860518a6c585c5bb6d` |
| 预算 | 1 次已消耗；5 次未发送；没有自动恢复或重试 |
| 行情覆盖、分支比较 | 未观测；没有生成行情分析或比较结论 |

完整请求、原始响应、开始前收据、完成收据和版本固定记录保存在 `ths_live_20260910/`。请求头、配置文件和令牌没有写入证据。计数是客户端 REST 请求次数，不代表插件内部可能的正式/后备调用次数；本次 401 未提供到达下游行情加载器的证据。

冻结的预期键另存于 `ths_live_20260910/expected_keys.json`：这三个样本按既有 manifest 与 calendar，各有研究 728 日、预热 250 日，共 978 个应有键。SH000300 按 benchmark 契约处理。这是合同计算，不是本机返回数据；不推及其余 49 个标的。

## 运行时证据和预检缺口

实际请求前确认只有一个目标宿主；已加载插件路径对应的磁盘文件 SHA-256 为 `19fbd89528ebe933cee267de34d63171afd47aef230132bd3574a3b81883469b`，未提取进程内模块字节。端口只监听 127.0.0.1。HTTP.sys 的监听 PID 4 通过唯一精确 URL 请求队列关联到 happ PID 29876；没有发现冲突的项目行情工作进程。这些证据证明目标进程身份，不能证明指定配置目录属于这个进程。

后续只读端点诊断如下，完整白名单字段见 `endpoint_identity_diagnosis.json`：

| 端点位置 | processId | startedAtUtc | 与当前宿主 |
|---|---:|---|---|
| `D:\TonghuasunCodex\runtime\endpoint.json` | 23512 | 2026-09-04T10:03:42.622042Z | 不匹配、陈旧 |
| `C:\Users\Administrator\AppData\Local\TonghuasunCodex\runtime\endpoint.json` | 29876 | 2026-09-09T16:09:03.75905Z | PID 匹配，晚于启动约 10.43 秒 |
| 当前 `happ.exe` | 29876 | 2026-09-09T16:08:53.328613Z | 宿主基准 |

**本轮验证器存在预检遗漏：检查了端口与宿主，却未核对项目端点文件的 PID、启动时间及更新时间。** 既有项目 `scripts/tonghuasun_readonly.ps1` 已有该身份检查（约 118–126 行）；`scripts/tonghuasun_readonly.psd1` 指定 D 盘目录。应复用这一边界，不能只因端口相同就使用陈旧端点目录的配置。

捕获时使用的验证器保持冻结，SHA-256 为 `1eb483b623458ac912d877e1f2cca6a7fb6bef343abbe35725374cc60b08ec67`；不把后续修正冒称为当时已生效。新增的独立端点身份门禁与回归测试用于后续接入，**本次 401 请求未使用它**，它的通过也不授权重试或清除已消耗预算。

## 代码与验证

- `ths_long_history_probe.py`：仅标准库；固定行情路径、证券与参数白名单；请求前持久保留预算；最大 8 MiB 响应；30 秒连接/读取截止时间及 socket watchdog；不跟随重定向，不自动重试；收到错误证券、口径回显或协议结构异常时停止。
- `test_ths_long_history_probe.py`：**42/42 合成测试通过**。最终捕获版本复跑耗时 0.486 秒，退出码 0，记录在 `root_offline_test_results_r2.txt`。覆盖 978 行、缺失/重复/异常值、证券与日期一致性、布尔类型白名单、六次预算及传输异常。真实 socket、子进程和凭据读取被阻断，捕获流程完全在内存模拟。
- `endpoint_identity_guard.py` 与独立测试：**20/20 通过**，root 复跑耗时 0.005 秒。纯数据检查覆盖 PID 类型/一致性、启动时间与文件时间、精确回环地址及缺失字段拒绝。`endpoint_identity_guard_observation.json` 将新门禁应用到只读端点元数据：D 盘记录因 PID、启动时间和文件时间三项拒绝，用户目录记录与保留的宿主身份匹配。没有读取用户目录令牌或改用该目录发请求；该检查发生在 401 之后。
- 独立 Codex 复核：验证器固定版本和最小 PowerShell 宿主修订均审查通过。实测后再次独立核对原始响应、预算停止、5 个生产者/合同哈希及 978 个应有键，未发现收据与原始结果矛盾。该复核并未使实际长历史验证通过。
- 首次启动仅在本机 PowerShell 5 预检阶段失败，尚未创建采集目录、读令牌或发行情请求。保留原程序后改用已验证的 PowerShell 7.6.5 绝对路径，重新运行 42 项测试；后续才发生上述唯一 401 请求。两种失败不能混为两次 REST 请求。

## 保留状态与恢复边界

本轮只写审查目录。生产代码、数据库、知识文件、源策略、客户端配置及令牌均未修改；没有启停服务、账户查询、交易操作、Git 暂存/提交/推送或 Claude 派单。实盘未启用。原两份 Claude 交付保持原字节，需求修订补丁仍未应用。

保全检查确认 91 个受保护文件、G1 的 46 个文件及路径集合、全部 Git 已跟踪文件与接管基线一致，暂存区为空。新检查记录为 `probe_preservation_after.json`；旧静态阶段清单独立保留。

本轮复用旧保全脚本时，其固定输出名更新了旧阶段的收尾收据。已将新检查保存为上述独立文件，并依据当前任务原始运行记录中的时间戳恢复旧收据；恢复后的完整字节 SHA-256 与旧清单固定值 `ce77fa1708078033703cd5e3958a12243c0cea7fcb111ddcda297c18172213a5` 完全一致。最终封存重新核对旧清单全部 13 项及本次捕获固定项，避免新阶段改写旧阶段证据。

P1 仍 open，U-6 仍 deferred，eligibility 仍 false；旧 Sina capability FAIL、EV6 及两次历史授权消耗记录全部保留。本次新的本机认证阻塞不是对旧证据的替换。M2 三年数据集尚未完成验收。

当前需要先恢复**项目配置目录对应的客户端实例**。应由用户正常退出当前同花顺，再经现有项目只读启动入口启动并由用户手动登录。不能自动杀进程、复制或轮换令牌，也不能用启动完整项目栈替代单独客户端恢复。

恢复后的最小入口是仅调用已存在的 `Get-TonghuasunReadOnlyContext` / `Start-TonghuasunReadOnlyClient`，让子进程继承 `TONGHUASUN_AGENT_HOME=D:\TonghuasunCodex`。本轮没有执行该启动动作。下一次实际请求前必须确认项目 endpoint PID/时间与当前宿主一致，并记录继续验证的明确范围；保留已消耗 1 次的事实，不清空当前捕获目录、不自动重跑第 1 次。其余 5 次只是未发出额度，不代表本轮停止之后已自动获准恢复。
