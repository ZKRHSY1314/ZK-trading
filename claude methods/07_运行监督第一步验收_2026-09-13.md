# 运行监督第一步：诊断与新鲜度防误报

任务状态：`validated`（Codex 实现及自测）；不是独立 Claude 复核或用户 accepted。日期：2026-09-13。承接 06 号建议复核的 P0 运行监督工作。

**本步交付完成：可在后端停运时使用的只读诊断入口，以及应用内空缓存/非法日期防误报修复。生产栈、定时监督和行情刷新尚未恢复，完整 A1 仍在推进中。**

## 当前原因与证据

- 系统最后启动时间为 2026-09-13 11:04:19（台北）；项目保存的进程元数据与心跳均早于此次启动，实际受跟踪进程不存在。
- `ZKTrading-ReviewOnly-ControlPlane` 任务未安装。已有监督脚本并不等于监督任务在运行；这解释了本次开机后没有自动恢复，但无法据此确定 9 月 4 日第一次退出的原因。
- `control_plane_loop.py` 对网络/OSError 异常捕获后会继续循环；旧 WinError 10054 不能独立证明进程因此退出。
- 缓存最新合法 ready 日期 9 月 4 日，仅 2,608 行；近期峰值为 5,553 行。9 月 11 日这一工作日代理目标为 0 行，另有一行非法日期。工作日代理落后 5 日，不冒充经过交易所日历确认的 5 个交易日。

## 实现

1. 新增 `backend/scripts/stack_diagnostics.py` 和 `scripts/check_stack.ps1`。固定 loopback GET，读取本机受跟踪进程、任务状态和心跳；数据库以只读 URI 与 query_only 打开，不导入 app，不启动 worker、不访问行情或账户接口。
2. `scripts/ensure_stack.ps1 -CheckOnly` 在加载同花顺 profile 及任何 stop/start 逻辑之前退出到只读诊断。保留用户原有 profile/source-policy 改动。
3. 将进程身份、心跳结果和行情覆盖分开。陈旧 completed、未知状态、未来心跳、runtime PID 不符均不能当正常。关闭的 Codex worker 单列 disabled；后端不可达时，实际 live-trading 状态是 null/未核实，不能伪报 false。
4. 修复 `DataFreshnessDiagnosticsService`：空缓存且无候选不再 ready；全局与候选聚合均排除 ERROR、无效日历日期、非规范 ISO 日期和未来日期。保留原自然日参数语义及既有 API 字段。
5. 新增 [操作与恢复边界说明](<D:/codex-A股交易/docs/STACK_DIAGNOSTICS.md>)。已列出整栈默认启动的写入能力，避免把“只读监督”误当作原 `ensure_stack` 的默认行为。

源代码写入范围：上述两个新增入口、ensure_stack 的 CheckOnly 分支、data_freshness 及相关测试。新增操作文档与本验收证据；没有修改其他业务源文件、冻结的 M3/M4 文件、研究数据或标签。原未提交内容保留，未暂存/提交/推送。

本地虚拟环境的 Ruff 从 0.15.20 对齐到仓库明确要求的 0.16.6；没有改动仓库要求版本或其他依赖。

## 验证

测试使用唯一临时 SQLite，显式 `ENABLE_LIVE_TRADING=false`，未把 pytest 指向生产库。

```text
python -B -m pytest -q
  tests/test_stack_diagnostics.py
  tests/test_run_stack_script.py
  tests/test_data_freshness_diagnostics.py
  tests/test_stability_diagnostics.py
  tests/test_tonghuasun_startup_script.py
  tests/test_runtime_health.py
结果：70 passed，13.86 秒；一个既有 Starlette TestClient 弃用提示。
```

最后收紧测试辅助进程的等待超时后，单独复跑真实 PID 身份测试：1 passed；不是额外一个独立用例。Ruff 0.16.6 对全部本轮 Python 修改通过，`git diff --check` 无错误（存在原有换行转换提示）。未复跑全量后端 suite，未宣称既有两个 M4 隔离失败已解决；前端无改动，未构建前端。

行为证明包括：空库不被创建；空行情不 ready；最新单条日线不能掩盖横截面缺口；非法与未来日期不能抬高最新日期；休市/收盘前代理有明确标签；陈旧/未来/未知心跳不通过；旧 completed 不掩盖死进程；心跳 PID 不符被拒；真实子进程使用错误创建时间被拒；CheckOnly 不能进入启动、停止或插件配置代码；输出路径不能覆盖数据库；故障正文不会泄露到诊断输出。

现场执行原样命令：

```powershell
powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File scripts/ensure_stack.ps1 -CheckOnly
```

实测返回码 **2**、`needs_attention`，符合当前后端停运/数据陈旧状态，而不是脚本失败或验收不通过。实际回执与报告位于 `_supervision_20260913/receipt.json`、`diagnostics.json`。

生产库前后 size=1,346,056,192、mtime 与 SHA-256 全部一致：`f9b4fe8792112ff15e91968162e42523ad9d07b8c0e29d92af550936a29a8d78`。

## 剩余项及下一步

- 本步未进行生产恢复、装定时任务、市场采集、脏行删除、Claude 委派或巡检恢复。原 A1 不得标整体完成。
- 独立诊断的交易日历与覆盖总体尚未认证，输出明确保留 proxy/unverified；不会把代理统计做成交易或训练资格。
- 下一步拆出受控启动组合：明确哪些 worker 启动、实际数据库/manifest 写入范围、行情来源回退、退出与回滚条件。先用临时数据库验证故障恢复，再对有备份的生产范围实施恢复，最后验证持久监督的实际触发与失败通知。默认 full/adaptive 可能进入模拟任务与派生记录，不能作为只恢复行情的最小组合。
- 后续依次解决 M4 测试隔离、canonical 预测口径和生产执行因果性；此次没有修改标签、规则阈值或研究资格。M3/M4 保持原有技术收口/证据未达标状态，M5 未启动。

## Next-stage Claude instructions — draft only, not dispatched

Independently review this bounded diagnostics increment and its tests. Verify that CheckOnly cannot reach startup, shutdown, plugin configuration or database mutation; preserve the distinction between process identity, cycle success and market-data freshness. Then review a minimal recovery profile with explicit worker and data-write scope, backup/rollback requirements and fault-injection acceptance cases. Do not start workers, install scheduled tasks, fetch market data, modify SQLite, skip frozen M4 tests or start M5 during review.
