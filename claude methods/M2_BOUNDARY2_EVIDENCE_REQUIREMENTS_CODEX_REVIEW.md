# Codex review — boundary-2 evidence requirements draft

2026-09-09. **Corrections required.** No implementation or operational permission follows from this review.

Reviewed requirements draft `7f1cbe9c82089ec3f9176b4a4c2ac2a4ba0d713ed1f5b404a122c6db295d647f` and dependency map `2b4546cd51ffd9dab3855c6fd3d33e411ecf8acca31dcb07cd251e2328a3bf21`. Frozen at `_m2_codex_review/boundary2_requirements_review_20260909_r1/` with independent preservation verification. Both documents' published pins match; 91 protected files and all 46 G1 files/path set match. HEAD `73f266d`; nothing staged. Claude's sole fork was observed Idle with matching final hashes. Static reads/hashing only; no tests, replay, decoder, database or network.

The map's current-body corrections to the historical audit scope and the partial G7 treatment are verified. Preserve these, the inherited gate mapping and all previous accepted work. The new draft has the following substantive issues; address them together rather than generating another layer of preparatory documents.

## BQ-R1 — Adapter return keys are not pilot acceptance keys

Draft section 3.3 says a revised request must not express index expectations as a window. That is true of the index adapter's interface-level R1 check, not of the pilot corpus. `smoke_checks.py:432-447` explicitly explains the interface distinction. `staging_gate.py:215-236` and `:535-541` derive listing-aware expected keys from the manifest/window for stocks **and benchmarks**, and the existing pilot request section 1 specifies the same research/warm-up intervals and consumed views. Keep separate (a) raw served keys, (b) actual adapter output keys, (c) expected eligible research/warm-up keys and (d) accepted staged-view keys. Receiving 5,987 index rows does not make all of them eligible for the pilot. Extra history must be accounted for without becoming extra research observations.

Likewise, unknown availability is not an unknown contract: the pinned calendar/listing rules define expected eligible keys; future served coverage determines whether they were supplied. A source's first returned date does not redefine the listing date, shrink the expected domain or justify missing keys. C2's 978 served sessions and pre-boundary history establish observed coverage for the sampled interface, not verified security identity for all historical dates or a new listing rule. State the current requirements without implementing a filtering route.

## BQ-R2 — Keeping unverified cannot discharge the basis prerequisite

Section 4's G7 row and section 7 decision 2 offer a decision to proceed with `unverified` as an alternative way to discharge the pilot's P1 prerequisite. This silently reopens U-1/U-2. The proposal section 5 U-1 (`:454-470`) explicitly excludes unverified data from basis-dependent boundary-2 use while permitting non-basis audit work; the current pilot request section 1 calls for raw/unadjusted views, whose `none` semantics are unchanged. The bounded adoption remains Claude-reported, not independently verified, but no later permission has reversed those semantics.

Retaining unverified audit records is a valid holding state, **not** an alternative pass through this pilot requirement. Remove the fallback as a way to proceed with the current basis-dependent pilot. Keep P1/U-6 blocked by the actual evidence/policy gaps; do not suggest a waiver or relaxing the agreed objective. Independent non-basis analysis can continue only within its own authorized scope and does not complete M2.

## BQ-R3 — Distinguish measured diagnostics from unobserved causes and effects

In section 3.2, zero `duplicate_ohlcva_rows` does not prove adapter de-duplication removed nothing. `smoke_checks.py:1383-1392` counts repeated OHLCVA tuples in decoded `in_window`, **before** and independently of the adapter replay. C4's `effective_first_date` is a computed diagnostic bound from first price/usable-share dates (`:1404-1411`), not a general proof of what the adapter retained. Share-only dates may themselves be valid trading sessions with missing price observations; their absence from this price payload does not prove they are non-trading dates. Do not promote them to price rows by forward-fill, but also do not claim their market-calendar status without evidence. Refer to the actual measurement stage and maintain the unresolved orphan-date cause.

Correct the future-population arithmetic: 52 total minus the sampled two stocks and one index leaves **49**, not 51, unobserved instruments relative to this sample. Preserve the distinction between the aborted capture and the single complete observation. Section 6's blanket `no run supplies these` incorrectly includes future per-symbol availability, which is an empirical observation. The cause of C2 is not automatically a required blocker: evidence of correct historical identity and coverage matters; explaining the vendor's internal mechanism is not by itself an adopted gate. Keep the adopted evidence requirements separate from optional causal explanations.

## BQ-R4 — Finish this requirements task without inventing another permission loop

The first item of section 7 asks again whether/by whom rev. 3 should be drafted, while section 1 calls this document merely the layer beneath it. The current task already authorized the non-executable evidence-requirements work. Complete its analytical content in this document, explicitly naming whatever later operational-request details remain genuinely blocked by evidence or route decisions. Do not ask the user to commission the same analysis again or propose a new document whose only purpose is to restate this one. This is not permission to add commands, endpoints, configurations, new policy or operational preparation.

Bring the dependency map's current decision list into agreement: remove the stale ask to initiate the already authorized draft; remove scope reduction as a way to finish M2; keep alternate pilot routes explicitly subject to their own design/review. Preserve historical revision entries as history, but clearly supersede old assertions when citing them. Qualify `no such check exists`/`no isolation mechanism` to the inspected current code; prior review explicitly did not establish repository-wide absence. Do not require every symbol to have a corporate action in the interval: an independently established action-free interval is a different evidence case, not proof of missing data and not automatic basis verification. No new threshold or sufficiency criterion should be invented.

## Next instruction to Claude

Task **M2-BOUNDARY2-REQUIREMENTS-CORRECTION-20260909**. Read this review in full and close BQ-R1 through BQ-R4 in one consolidated pass. Edit only `claude methods/M2_BOUNDARY2_EVIDENCE_REQUIREMENTS_DRAFT.md` and the corresponding current-state/decision statements in `claude methods/M2_REMAINING_DEPENDENCY_DECISIONS.md`. Use the named existing source and review documents; direct Markdown edits and bounded static reads/hash verification only. Preserve the current non-executable scope and finish the already commissioned analytical deliverable. No new documents beyond these two, scripts, directories, studies, tests/imports/runtime/replay/decoder, database/dataset/private/demo fixture access, network/plugin acquisition, capture preparation, service actions, production/data/schema/strategy/knowledge changes, policy adoption, gates/labels/thresholds/eligibility changes, pilot/backfill/training, Git actions, agents or workflows. Preserve all accepted artifacts and P1 open/U-6 deferred/all eligibility false/capability FAIL, immutable EV6 and both spent capture grants. Codex owns email and automation. Deliver two stable hashes, actual consulted pins and preservation evidence at proposed for review; do not mark these findings closed yourself or claim M2 complete.
