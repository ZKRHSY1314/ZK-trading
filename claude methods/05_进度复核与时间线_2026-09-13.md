# 进度复核与剩余时间线 — 2026-09-13

审阅人：Claude（独立复核，只读）。基线：分支 `codex/control-plane-refactor`，HEAD `73f266d`（09-04），工作区 28 个修改文件 + 23 个未跟踪文件。
方法：读 M1–M4 验收文件、直接查生产库、重跑回测样本与全量测试。没有修改任何项目文件，没有写库，没有提交。

---

## 0. 一页结论

1. **09-03 之后十天，项目分成了两条线**：生产驾驶舱（Control Plane / 前端 / forecast ledger）和一个新的三年研究计划（M0–M8，`THREE_YEAR_RESEARCH_EXECUTION_GOAL.md`）。研究线 7 天内技术收口了 M1–M4，但 **M3、M4 的证据目标都未达成**；生产线 **09-04 起停机 9 天，无人发现**。
2. **上一轮清单的 P0 全部落地**：成交额缺失 97.6% → 10.6%，出场规则/ARMED 状态机/中间档位进了 `rules.yaml` 和引擎（有测试），知识库已导入，Node 已装。
3. **一个重要的自我修正**：09-01 报告的 h=1 Rank IC −0.169 是 30% 覆盖率下的假象。补齐到 93% 覆盖后 **h=1 IC +0.004、h=5 +0.081、h=10 +0.047、h=20 −0.035**。结论从"信号反向"改为"**没有证据支持任何方向**"。依然只有 6 个决策日。
4. **研究线的瓶颈已经不是工程，是数据授权**：50 只股票的固定总体在 M3 已被穷尽（双审正例 0/50），M4 的历史资格路径 0 意图 0 成交。不扩大总体，M5–M8 无法启动。
5. **时间线**：生产线做到"每天自动跑 + 记分板"约 **3–4 周**；研究线到 M8 约 **10–14 周**，前提是用户在本周内授权扩大总体；若维持 50 只，研究线**没有可估算的终点**。

---

## 1. 里程碑状态（研究线 M0–M8）

| 里程碑 | 目标 | 状态（截至 09-13） | 判定依据 |
|---|---|---|---|
| M1 数据契约与只读就绪 | 三年区间、总体、质量门 | ✅ `validated`（09-06） | 67 项端到端、46 项质量检查全过 |
| M2 授权历史回填 | 暂存 + 验证 | ✅ `validated`（09-10），**范围仅 50 股 + 2 指数** | 45,685 价格行/库，两库一致；未迁生产；同花顺单响应最多 978 根，长历史能力已证明 |
| M2b Sina/AkShare 来源能力 | 来源资格 | ❌ `FAIL`（EV6 两只标的解析分歧），两次抓取授权已耗尽，P1 OPEN | §15 权威状态框 |
| M3 标签与案例库 | ≥50 双审正例 | ⚠ `technically_closed_evidence_target_not_met`：**0/50**，32 争议；`training_eligible=false` | 32 个有对照的阶段已穷尽；Codex 31 正例 vs Claude 0 正例 |
| M4 回测引擎有效性 | 六项验收 | ⚠ `technically_closed_evidence_target_not_met`：合成证明通过；**历史资格路径 0 意图 0 成交**；假设分支 full_fill −4.56% / fixed_5000 +5.04%（50 股，2023-09→2025-03） | `M4_complete=false`；六项中"历史当时可得性 / 真实 ST 价格带费率 / 幸存者 / 合格历史非零交易"未满足 |
| M5 基线模型 | — | ⬜ 未启动，指令为 "remain stopped" | |
| M6 走样验证 | — | ⬜ | |
| M7 经验记忆 | — | ⬜ | |
| M8 纸面运行 | — | ⬜ | |

**研究线的真实位置**：工程 4/8，证据 2/8。M3、M4 的"技术收口"文件本身写得很诚实——它们明确说"不得用合成证明代替合格历史证据"。问题是下一步依赖一个尚未做出的用户决定（见第 5 节）。

**一个要指出的成本**：M2b 一个子阶段产出了约 40 份复核/验收文件，`THREE_YEAR_RESEARCH_EXECUTION_GOAL.md` 235 KB，`claude methods/` 累计 **3.6 GB**（其中 M2 暂存 2.6 GB）。复核轮次（R1→R2→R3）正在吞掉推进时间；EV6 两只标的的解析分歧不值得再来一轮抓取授权——生产缓存已经用 Sina 写了 240 万行。

---

## 2. 生产线状态（驾驶舱）

### 2.1 运行

| 项 | 实测 |
|---|---|
| Control Plane | **09-04 17:46 `failed`**（cycle 32，`WinError 10054` 连接被重置），此后无心跳；今天 09-13 |
| 日线 | 最新完整横截面 **09-03**（5,552 只）；09-04 只有 2,608 只；**过期 9 天** |
| 模拟盘 | `simulation_fills` 仍为 **0** |
| 基本面快照 | 仍是 08-31 那一份，**13 天未刷新**，无调度 |
| capital_flow worker | 仍在对 eastmoney 失败（`degraded`，cycle 11） |
| 一个坏行 | `daily_bar_cache` 有一行 `symbol=BJ920289, trade_date='ERROR'` |

**没有任何告警机制发现停机 9 天**——这比停机本身更值得修。

### 2.2 上一轮清单落地情况

| 项 | 状态 | 证据 |
|---|---|---|
| P0-1 回测能成交 | ✅ 已提交（`27118cf`） | |
| P0-2 tier 可达性 | ✅ | `watch` 现在单条 `dengzhan_low_position_limit_up` 即可达，中间档存在 |
| P0-新 `amount` 缺失 | ✅ **97.6% → 10.6%** | `akshare.stock_zh_a_daily`（Sina）2,426,883 行，成交额齐全 |
| 出场规则归位 | ✅ | `rules.yaml` `exit_rules`：6% 止损、15% 减半、破 5 日线且破涨停均价、`max_holding_days: null`；引擎硬编码 8%/15%/5 天已删；3 个测试 |
| 状态机 | ✅ | `armed_window_days: 5`，引擎按 `same_bar_s0` 武装、窗口内允许分歧入场；有测试 |
| P1-1 导入数据集1 | ✅ | principles 13 / trade_cases 27 / strategies 26 |
| 环境 Node | ✅ | `C:\Program Files\nodejs` |
| P0-3 负 IC 进 release gate | ❌ | `stability.py` / 路由均未读 IC |
| 记分板首页 | ❌ | `TradingDashboard.vue` 无 IC/胜率 |
| P1-2 未跟踪文件 | ❌ **恶化**：23 个未跟踪（约 15.5k 行 M3/M4 模块+测试）+ 28 个修改，**09-04 起无提交** | |
| P1-3 停掉失败 worker | ❌ | capital_flow 仍在跑 |
| P2-1 删死代码 | ❌ | `LegacyConsole.vue` 25,526 行仍在；`backend/app` **134k → 144.6k 行**，路由 571 |

### 2.3 Forecast Feedback：修正后的数字

h=5/10/20 的 outcome 已从 0 补到 3,630 条（K 线刷新后自动标注，与上轮预判一致）。同一批 6 个决策日、121 folds：

| 周期 | n | Rank IC | P@5 | Top5 超额 / 胜率 | 全体超额 |
|---|---:|---:|---:|---|---:|
| h=1 | 3630 | **+0.004** | 0.583 | +0.18% / 58% | −0.4% |
| h=3 | 3630 | −0.049 | 0.041 | — | — |
| h=5 | 3630 | +0.081 | 0.180 | −10.4% / 18% | **≈ −10%（各档一样）** |
| h=10 | 3630 | +0.047 | 0.415 | −8.8% / 41% | ≈ −9% |
| h=20 | 3630 | −0.035 | 0.299 | −2.4% / 30% | ≈ −2.5% |

- **09-01 的 −0.169 不成立**：当时只有 30% 的 outcome 到期（1,074 条），恰好是先到期的那批。补齐后 h=1 归零。我在 `01_诊断报告` 里的"信号反向"结论**撤回**，改为"无证据"。
- h=5 各档一起 −10%：这是 7 月中旬那一周入选的强势股整体跑输指数，是**队列效应**，不是排序能力。
- 板块 h=20 IC +0.339（n=106）是唯一看起来有意思的数，但 106 个样本来自同一周。
- 结论不变：**6 个决策日不足以说任何话**，不能拿这些数调参数。

### 2.4 状态机在真实数据上的首次运行

同一脚本 `verify/backtest_sample_250.py`，250 只，2025-09 → 2026-07，`allow_projected_fundamentals=True`：

```
S0 pass = 4     entry_signal = 1     fill = 1     closed = 1     total_return −1.12%
liquidity_basis = reported_amount（不再是代理）
```

与 09-03 的 4 signal / 4 fill **不可直接比较**：回填后 universe 从 5,050 变大，抽样落到了不同的 250 只。状态机在夹具测试里通过，但**尚未在同一样本上做过 A/B**（同根 K 线 vs 5 日窗口，全市场统计是 77 vs 124）。这是 Codex 该做的第一个策略验证任务。

### 2.5 数据源混用检查（新做）

`tonghuasun_first` 每天为约 613 只候选写入生产缓存，`TONGHUASUN_SETUP.md` 说"部分历史前复权价格不一致、单位未正式确认"。我做了一个直接检验：同一只股票在来源切换的那根 K 线上，|日收益| > 11% 的比例为 **0.76%**，同来源相邻 K 线为 1.15%；> 21% 的 0 vs 0.03%；最大的几个跳变全是北交所 30% 涨跌幅品种。**没有发现复权基准跳变的证据**。可以继续用，但正式的量额单位验收仍应补。

### 2.6 测试

- 全量：**1,034 passed，2 failed**（3 分 24 秒；09-03 是 582）。
- 两个失败都是 M4 的隔离断言 `assert "app" not in sys.modules`——`conftest.py` 本身就 import `app.main`，所以**单跑通过、全量必挂**。是测试顺序依赖，不是代码缺陷，但它让 CI 红了。
- 协作文档指定的两组入口：39 passed、83 passed。

---

## 3. 剩余工作与时间估算

前提假设：Codex 按 09-06 → 09-12 的速度推进（7 天技术收口 4 个里程碑），复核轮次控制在每阶段 ≤2 轮，用户在 1–2 天内回应授权请求。估算是**区间**，不是承诺。

### 3.1 生产线 —— 到"每天自动跑、首页是记分板"

| # | 任务 | 工时 | 依赖 |
|---|---|---|---|
| A1 | 重启 stack；修 `ERROR` 行；加"K 线过期 > 2 个交易日"红条 | 1 天 | 无 |
| A2 | 未跟踪 15.5k 行分 3 个 commit 落盘；修 2 个顺序依赖测试 | 1 天 | 无 |
| A3 | P0-3：IC/覆盖率进 release gate；记分板首页（最近 60 决策日超额、胜率、按 h 分开） | 4–5 天 | A1 |
| A4 | 状态机同样本 A/B + 三个对照组（随机 / 取反 / 单因子）在现有 2.3 年上跑通 | 3–4 天 | A2 |
| A5 | 基本面快照进 4 小时 worker；摘掉 capital_flow；`LegacyConsole.vue` 删除 | 2 天 | A1 |
| A6 | `dataset2_readiness` / `trade_execution/gateway` / `screen_monitoring` 进 `legacy/`，CI 覆盖实际注册路由 | 5–7 天 | A2 |

**合计 3–4 周**。A1、A2 是本周必须做的；A6 可以拖到最后。

### 3.2 研究线 —— M3/M4 补证据 → M8

| # | 任务 | 工时 | 依赖 |
|---|---|---|---|
| B0 | **用户决定**：是否授权把研究总体从 50 只扩到 ≥500 只（同花顺长历史，已证明单响应 978 根） | 0（决定） | — |
| B1 | 扩大总体的抓取 + M1/M2 同款验证（暂存、不迁生产） | 1.5–2 周 | B0 |
| B2 | M3 在新总体上重做阶段盘点与双审（目标 ≥50 正例） | 1.5–2 周 | B1 |
| B3 | M4 历史资格补证：ST/价格带/费率生效期/退市/公司行动 | 1.5–2 周 | B1（可与 B2 并行）|
| B4 | M5 基线（规则排序、阶段分类、固定信号上的出场策略、两个基准） | 2 周 | B2、B3 |
| B5 | M6 走样 + 状态分层 + 敏感性 + 拒绝准则 | 2–3 周 | B4 |
| B6 | M7 经验记忆（幂等、有来源） | 1–2 周 | B5 |
| B7 | M8 纸面运行（与生产线 A3 合流） | 2 周 + 持续 | B6、A3 |

**合计 10–14 周（到 12 月初 / 12 月中）**，且 B1 之前的每一周都是纯等待。
**若 B0 决定为"不扩"**：M3 已穷尽、M4 无合格历史成交，M5 起没有可估的路径；研究线应改为"只做生产线 + 前向记账"。

### 3.3 两条线的关系

生产线的 A4（对照组）和研究线的 B4（M5 基线）是同一件事的两个版本。建议**以生产线的回测引擎为准**做 A4，M4 的隔离内核只在 M3/M4 证据补齐后用于走样验证；不要并行维护两套执行模型到年底。

---

## 4. 风险

| 风险 | 现状 | 缓解 |
|---|---|---|
| 停机无人知 | 9 天 | A1 的过期红条；`ensure_stack` 定时任务 |
| 未提交代码丢失 | 15.5k 行、9 天 | A2 本周做 |
| 复核轮次膨胀 | M2b 40 份文件、goal 文件 235 KB | 每阶段 ≤2 轮；超过就升级给用户裁决，不再 R3/R4 |
| 证据目录膨胀 | 3.6 GB，部分在 git exclude 外 | 把 `_m1_evidence`、`_m2_*` 也加进 `.git/info/exclude`；大件移到 `claude methods/` 之外 |
| 两套执行模型并存 | `app/backtest/engine.py` vs `app/research/m4_*` | 3.3 节的合流约定 |
| 拿 6 个决策日的数调参 | 诱惑存在（h=5 IC +0.08） | ≥300 笔 / ≥3 状态前不动阈值 |

---

## 5. 下一步建议（按顺序，前三件本周）

1. **今天重启 stack 并加过期告警**（A1）。停机 9 天而系统自己不知道，是当前最大的运营缺陷。
2. **本周把 09-04 以来的工作提交**（A2），顺手把两个 M4 隔离测试改成子进程执行或在 `app` 已加载时 skip，让 CI 回绿。
3. **用户做 B0 决定**：研究线的全部剩余时间线都挂在"扩不扩总体"上。我的建议是**扩**，因为 M2 已经证明同花顺长历史能力，而 50 只总体在 M3 已被证明不够；同时**停止 M2b 的 EV6 追加轮次**，Sina 已经是生产路径的主来源。
4. **状态机同样本 A/B + 三个对照组**（A4），Codex 负责实现，Claude 复核口径。这是当前唯一能用现有数据回答的策略问题。
5. **记分板上首页**（A3）。研究线在等授权的这两周，正好是做它的窗口。
6. 不做的事：不调 `rules.yaml` 任何阈值；不启动 M5；不再开新的选股链路；不为 EV6 两只标的申请第三次抓取。

---

## 6. 本次复核的实际操作

- 只读查询 `trading_local.sqlite3`；重跑 `verify/backtest_sample_250.py`（`persist=False`）；全量 pytest 两次；协作文档指定的两组测试各一次。
- 未修改任何项目文件；未写库；未提交。本文件是唯一新增产物。
- 全部数字可用 `claude methods/verify/` 下脚本重算。以上为软件、数据与研究方法建议，不构成投资建议。
